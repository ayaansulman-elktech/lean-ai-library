[
  {
    "slug": "agent",
    "id": "agent",
    "name": "agent-name-here",
    "type": "agent",
    "category": "agents",
    "description": "What the agent achieves AND when to use it.",
    "shortDescription": "agent-name-here",
    "keywords": [],
    "githubPath": "_templates/agent",
    "skill": "# <Agent Name>\r\n\r\nAn agent is a skill **plus** a loop, memory, tools, and planning.\r\n\r\n## Loop\r\n- Goal: ...\r\n- Terminates on: success | failure | budget | human interrupt.\r\n\r\n## Memory\r\n- Short-term: within-run scratch.\r\n- Long-term: what persists across runs, and where.\r\n\r\n## Tools\r\n- the tools / `scripts/` this agent may call.\r\n\r\n## Planning / reflection\r\n- how it decomposes the task and self-checks before finishing.\r\n",
    "assets": [],
    "examples": [],
    "dependencies": [],
    "related": [],
    "contentPath": "",
    "coverPath": "assets/library-card-cover.png",
    "coverPreviewPath": ""
  },
  {
    "slug": "agent-core",
    "id": "agent-core",
    "name": "agent-core",
    "type": "agent",
    "category": "agents",
    "description": "The factory's **shared agent framework**, built on `ml-core`. A thin, production-minded base so every agent (wireframing, compliance, security, ux-audit) is consistent: budget-checked, cost-metered, observable, with structured output and memory.",
    "shortDescription": "agent-core",
    "keywords": [],
    "version": "0.1.0",
    "githubPath": "libraries/agent-core",
    "readme": "# agent-core\r\n\r\nThe factory's **shared agent framework**, built on `ml-core`. A thin, production-minded base\r\nso every agent (wireframing, compliance, security, ux-audit) is consistent: budget-checked,\r\ncost-metered, observable, with structured output and memory.\r\n\r\n## What it gives you\r\n- **`Agent`** base — subclass it and implement `_run(task) -> output`. `run()` wraps that in the\r\n  contract: budget enforcement, step counting, cost accounting, MLflow tracking, and failures\r\n  returned as an `AgentResult` (never an uncaught crash).\r\n- **`Agent.llm(...)` / `Agent.llm_json(...)`** — budget-checked LLM calls; `llm_json` returns a\r\n  parsed (optionally pydantic-validated) JSON object with corrective retry.\r\n- **Memory** — `ShortTermMemory` (per run) + a `MemoryStore` protocol (`InMemoryStore` default;\r\n  back it with sqlite/files in prod).\r\n- **Tools** — a typed `ToolRegistry` of deterministic callables agents orchestrate.\r\n\r\n## Minimal agent\r\n```python\r\nfrom ml_core import load_providers, CostMeter, build_gateway\r\nfrom agent_core import Agent, AgentContext\r\n\r\nclass Summarize(Agent):\r\n    name = \"summarize\"\r\n    def _run(self, task: str) -> dict:\r\n        return self.llm_json(\r\n            system=\"You summarize. Reply JSON: {\\\"summary\\\": <=8 words}.\",\r\n            user=task,\r\n        )\r\n\r\ncfg = load_providers(\"../ml-core/providers.yaml\")\r\nctx = AgentContext(gateway=build_gateway(cfg, m := CostMeter(budget_usd=1.0), offline=True),\r\n                   meter=m, default_model=\"deepseek\")\r\nresult = Summarize(ctx).run(\"the cat sat on the mat\")\r\nprint(result.success, result.output, result.cost_usd)\r\n```\r\n\r\n## Design\r\n- **Built on ml-core** — all LLM calls go through the shared gateway/meter; agent-core adds the\r\n  loop/contract/memory/tools, nothing provider-specific.\r\n- **Injected context** — `AgentContext` makes agents fully testable with the mock gateway (no keys).\r\n- **Budget-first** — every LLM call checks the meter; over budget → `BudgetExceeded`, surfaced as\r\n  a failed `AgentResult`.\r\n\r\n## Test (offline, no keys)\r\n```bash\r\ncd libraries/agent-core\r\npython -m pytest          # mock gateway via ml-core; no providers, no spend\r\n```\r\n",
    "assets": [],
    "examples": [],
    "dependencies": [
      "pydantic>=2.6"
    ],
    "related": [],
    "contentPath": "",
    "coverPath": "assets/library-card-cover.png",
    "coverPreviewPath": ""
  },
  {
    "slug": "agents",
    "id": "agents",
    "name": "Subagent Architecture (V1)",
    "type": "agent",
    "category": "agents",
    "description": "Dev-time **subagents** for the factory — specialized Codex agents that Codex spawns in parallel and collects into one result (codebase exploration, review, verification). Reference: Codex *Subagents* docs.",
    "shortDescription": "Subagent Architecture (V1)",
    "keywords": [],
    "githubPath": ".codex/agents",
    "readme": "# Subagent Architecture (V1)\r\n\r\nDev-time **subagents** for the factory — specialized Codex agents that Codex spawns in parallel\r\nand collects into one result (codebase exploration, review, verification). Reference:\r\nCodex *Subagents* docs.\r\n\r\n> **Not the same as `agents/`.** `agents/` holds the factory's **product agents** (Python packages\r\n> on `agent-core` that ship in the pipeline). Subagents here are **dev-workflow orchestration** —\r\n> they operate *on this repo* during development. Different layer, different purpose.\r\n\r\n## Folder layout\r\n\r\n```\r\n.codex/\r\n├── config.toml            # [agents] global orchestration settings (max_threads, max_depth)\r\n└── agents/\r\n    ├── README.md          # this doc\r\n    └── _template.toml     # reference subagent — copy it to add one\r\n```\r\n\r\nOne `.toml` file = one subagent. Codex identifies it by its `name` field (match the filename by\r\nconvention). Project-scoped agents live here; personal ones go under `~/.codex/agents/`.\r\n\r\n## Config schema\r\n\r\nEvery subagent file **must** define:\r\n\r\n| Field | Required | Purpose |\r\n|---|:---:|---|\r\n| `name` | ✔ | how Codex spawns/refers to the agent |\r\n| `description` | ✔ | when Codex should use it |\r\n| `developer_instructions` | ✔ | the agent's core behavior |\r\n\r\nOptional (inherit from the parent session when omitted): `model`, `model_reasoning_effort`,\r\n`sandbox_mode` (`read-only` / `workspace-write`), `mcp_servers`, `nickname_candidates`,\r\n`skills.config`.\r\n\r\n## Global settings (`.codex/config.toml`)\r\n\r\n```toml\r\n[agents]\r\nmax_threads = 6   # concurrent open agent threads (default 6)\r\nmax_depth   = 1   # nesting depth; 1 = a child can spawn but not grandchildren (default 1)\r\n```\r\nKeep `max_depth = 1` unless you truly need recursive delegation — raising it turns broad\r\ndelegation into repeated fan-out (more tokens, latency, resource use).\r\n\r\n## Suggested subagents (add as needed)\r\n\r\nNone are shipped yet — create them from `_template.toml` when a workflow calls for it. Keep each\r\nnarrow and opinionated, one job, `read-only` unless it must run something. Good first candidates for\r\nthis repo:\r\n\r\n- an **explorer** — maps the monorepo (`libraries/`, `agents/`, `libraries/models/blocks/`), cites\r\n  files, proposes no fixes.\r\n- a **reviewer** — correctness, security, and **missing offline tests** (the factory's bar).\r\n- a **verifier** — runs the offline suites (`scripts/test-all.sh` or a package's `pytest`).\r\n\r\n## Add a subagent\r\n\r\n```\r\ncp _template.toml <name>.toml    # then set name/description/developer_instructions\r\n```\r\nCodex spawns subagents only when explicitly asked, e.g.:\r\n> \"Have `explorer` map the affected blocks, `reviewer` find real risks, and `verifier` run the\r\n> offline suites — then summarize each.\"\r\n",
    "assets": [],
    "examples": [],
    "dependencies": [],
    "related": [],
    "contentPath": "",
    "coverPath": "assets/library-card-cover.png",
    "coverPreviewPath": ""
  },
  {
    "slug": "ai-farm",
    "id": "ai-farm",
    "name": "AI Farm",
    "type": "library",
    "category": "code-library",
    "description": "Productionized models, each packaged as an MCP-compatible capability.",
    "shortDescription": "AI Farm",
    "keywords": [],
    "githubPath": "libraries/ai-farm",
    "readme": "# AI Farm\r\n\r\nProductionized models, each packaged as an MCP-compatible capability.\r\n\r\n## Raw → Ready\r\n\r\n- `raw/<model>/` — the model + a wrapper + a one-page card (inputs/outputs/license).\r\n- `ready/<model>` — the Apple-native build (CoreML / MLX) when an app needs it on-device.\r\n\r\nPromote `raw → ready` only when an app actually needs it on device.\r\n\r\n## registry.json\r\n\r\nThe single index agents read to discover capabilities. One entry per model:\r\n\r\n```json\r\n{ \"name\": \"\", \"task\": \"\", \"status\": \"raw | ready\", \"source\": \"\", \"license\": \"\", \"mcp\": \"\" }\r\n```\r\n\r\n## Phase-1 scope\r\n\r\nOnly `skin-tone-cv` goes fully Raw→Ready (the reference example). `SUM` / `ViNet`\r\nstay raw (used by `ux-audit` on host). Everything else is added on demand — do not\r\npre-fill the shelf.\r\n",
    "assets": [],
    "examples": [],
    "dependencies": [],
    "related": [],
    "contentPath": "",
    "coverPath": "assets/library-card-cover.png",
    "coverPreviewPath": ""
  },
  {
    "slug": "apple-compliance",
    "id": "apple-compliance",
    "name": "apple-compliance",
    "type": "agent",
    "category": "agents",
    "description": "See README.md for details.",
    "shortDescription": "Block for apple-compliance",
    "keywords": [
      "apple compliance"
    ],
    "version": "0.1.0",
    "githubPath": "libraries/agents/blocks/apple-compliance",
    "readme": "# Apple Compliance Agent\r\n\r\n**Agent Core:** `apple-compliance`\r\n\r\n## Overview\r\nThe Apple Compliance Agent is an automated gatekeeper designed to enforce Apple's App Store Review Guidelines, with a specific focus on **Guideline 4.3 (Spam)** and design minimums.\r\n\r\nBefore any Sprint App is shipped to the App Store, this agent reviews the UI/UX flows and feature sets to ensure the application provides enough unique value and adheres to Apple's strict minimum functionality requirements.\r\n\r\n## Architecture\r\n- Subclasses the core `Agent` framework from `libraries/agent-core`.\r\n- Plugs directly into the `sprint` pipeline to act as a go/no-go gate.\r\n- Operates offline and keylessly during local CI testing.\r\n\r\n## Capabilities\r\n- **Static Analysis:** Evaluates the product charter against known App Store rejection patterns.\r\n- **Design Review:** Flags applications that look like simple web wrappers or duplicate existing functionality without added value.\r\n",
    "skill": "# Apple Compliance gate\r\n\r\nThe existential gate for a high-volume app factory: **Guideline 4.3 (Spam)** bans near-duplicate\r\napps that differ only cosmetically. This agent (`src/apple_compliance/`, on `agent-core`) reviews\r\nan app descriptor and returns a structured `ComplianceReport`.\r\n\r\nTwo layers (like security-review):\r\n- **Deterministic checks** (`checks.py`, no LLM) — a **real 4.3 duplicate scan** that measures\r\n  lexical similarity of the app against its **sibling catalog** (not a self-reported \"how I differ\"\r\n  field), plus 2.3 metadata, 4.2 native/min-functionality (web-wrapper detection), 3.1 external\r\n  payments, and 5.1 privacy. Any **high-severity** finding **hard-blocks** regardless of the model.\r\n- **LLM review** for the judgement calls, given the deterministic findings as context.\r\n\r\n```python\r\nfrom apple_compliance import AppleComplianceAgent\r\nagent = AppleComplianceAgent(ctx, catalog=[sibling1, sibling2, ...])   # catalog powers the 4.3 scan\r\nres = agent.run({\"name\": ..., \"value\": ..., \"features\": [...], \"category\": ...})\r\nif not res.output[\"passes\"]:   # gate: block submission\r\n    ...\r\n```\r\nThe sibling catalog can also be passed per-run as the task's `\"siblings\"` key.\r\n\r\n- `verdict`: pass | flag | fail · `risk_level`: low | medium | high · `findings[]` (LLM + deterministic).\r\n- `static_findings[]`: the deterministic-only findings; any `high` here hard-blocks.\r\n- `passes()` blocks on a `fail`, any `high` overall risk, or any high-severity deterministic finding.\r\n- Canonical source: developer.apple.com/app-store/review/guidelines (see `references/guidelines.md`).\r\n",
    "assets": [],
    "examples": [],
    "dependencies": [
      "pydantic>=2.6"
    ],
    "related": [],
    "contentPath": "",
    "coverPath": "assets/library-card-cover.png",
    "coverPreviewPath": ""
  },
  {
    "slug": "apple-docs-rag",
    "id": "apple-docs-rag",
    "name": "apple-docs-rag",
    "type": "agent",
    "category": "agents",
    "description": "See README.md for details.",
    "shortDescription": "Block for apple-docs-rag",
    "keywords": [
      "apple docs rag"
    ],
    "version": "0.1.0",
    "githubPath": "libraries/agents/blocks/apple-docs-rag",
    "readme": "# Apple Docs RAG Agent\r\n\r\n**Agent Core:** `apple-docs-rag`\r\n\r\n## Overview\r\nThe Apple Docs RAG (Retrieval-Augmented Generation) Agent grounds LLM responses using official Apple documentation. It ensures that any technical or design advice provided to the sprint teams strictly adheres to Apple's Human Interface Guidelines (HIG) and developer documentation.\r\n\r\n## Architecture\r\n- Subclasses the core `Agent` framework from `libraries/agent-core`.\r\n- Leverages the dependency-free `rag` library (TF-IDF retrieval core) to index and search Apple Developer documentation.\r\n- Supports offline mocked responses for local CI without needing LLM API keys.\r\n\r\n## Capabilities\r\n- **Citations:** Every generated answer includes exact citations back to the source Apple documentation.\r\n- **Pluggable Retrieval:** Currently relies on a lightweight TF-IDF corpus, built to easily swap to embedding-based retrieval in the future.\r\n",
    "skill": "# Apple-docs RAG\r\n\r\nGrounds agents in real Apple docs instead of letting them hallucinate. Retrieves the most\r\nrelevant chunks from `corpus/` and answers **using only that context**, with `[Title]` citations;\r\nreturns `grounded: false` (\"Not found in the indexed Apple docs.\") when the corpus doesn't cover it.\r\n\r\n```python\r\nfrom apple_docs_rag import AppleDocsRAGAgent\r\nres = AppleDocsRAGAgent(ctx).run(\"Does Core ML run on device?\")\r\nres.output[\"answer\"]      # with [Core ML] citation\r\nres.output[\"grounded\"]    # False if not in the corpus\r\nres.output[\"sources\"]     # the source URLs\r\n```\r\n\r\n- **Corpus** (`corpus/*.md`): App Store guidelines (4.3-focused), Core ML, Foundation Models, HIG.\r\n  Add more docs by dropping `*.md` files (a `source:` line becomes the citation URL).\r\n- **Retrieval**: `libraries/rag` TF-IDF (no deps, offline). Pluggable seam for an embedding\r\n  retriever later (semantic recall via the ml-core providers).\r\n",
    "assets": [],
    "examples": [],
    "dependencies": [
      "pydantic>=2.6"
    ],
    "related": [],
    "contentPath": "",
    "coverPath": "assets/library-card-cover.png",
    "coverPreviewPath": ""
  },
  {
    "slug": "apple-guidelines",
    "id": "apple-guidelines",
    "name": "apple-guidelines",
    "type": "library",
    "category": "knowledge",
    "description": "A lean-ai-factory block.",
    "shortDescription": "Block for apple-guidelines",
    "keywords": [
      "apple guidelines"
    ],
    "version": "0.1.0",
    "githubPath": "libraries/knowledge/blocks/apple-guidelines",
    "assets": [],
    "examples": [],
    "dependencies": [],
    "related": [],
    "contentPath": "",
    "coverPath": "assets/library-card-cover.png",
    "coverPreviewPath": ""
  },
  {
    "slug": "article-1",
    "type": "pdf",
    "category": "agentic",
    "name": "Language Model",
    "shortDescription": "Skill optimization process",
    "description": "Cognitive Shift transforms cutting-edge AI research from MIT and the best practices developed by leading Silicon Valley companies into directly applicable frameworks.",
    "keywords": [
      "agentic",
      "llm",
      "security",
      "skill"
    ],
    "contentPath": "content/articles/article-1/content.pdf",
    "coverPath": "content/articles/article-1/cover.pdf",
    "coverPreviewPath": "content/articles/article-1/cover-preview.png"
  },
  {
    "slug": "article-2",
    "type": "pdf",
    "category": "agentic",
    "name": "Language Model",
    "shortDescription": "Skill optimization process",
    "description": "Cognitive Shift transforms cutting-edge AI research from MIT and the best practices developed by leading Silicon Valley companies into directly applicable frameworks.",
    "keywords": [
      "agentic",
      "llm",
      "security",
      "skill"
    ],
    "contentPath": "content/articles/article-2/content.pdf",
    "coverPath": "content/articles/article-2/cover.png",
    "coverPreviewPath": ""
  },
  {
    "slug": "article-3",
    "type": "pdf",
    "category": "agentic",
    "name": "Language Model",
    "shortDescription": "Skill optimization process",
    "description": "Cognitive Shift transforms cutting-edge AI research from MIT and the best practices developed by leading Silicon Valley companies into directly applicable frameworks.",
    "keywords": [
      "agentic",
      "llm",
      "security",
      "skill"
    ],
    "contentPath": "content/articles/article-3/content.pdf",
    "coverPath": "content/articles/article-3/cover.pdf",
    "coverPreviewPath": "content/articles/article-3/cover-preview.png"
  },
  {
    "slug": "blocks",
    "id": "blocks",
    "name": "Model blocks",
    "type": "model",
    "category": "models",
    "description": "Each model is **one self-contained folder** — the V1 architecture in [`../ARCHITECTURE.md`](../ARCHITECTURE.md). This folder holds the blocks; `_template/` is the canonical starting point (copy it to add a model).",
    "shortDescription": "Model blocks",
    "keywords": [],
    "githubPath": "libraries/models/blocks",
    "readme": "# Model blocks\r\n\r\nEach model is **one self-contained folder** — the V1 architecture in\r\n[`../ARCHITECTURE.md`](../ARCHITECTURE.md). This folder holds the blocks; `_template/` is the\r\ncanonical starting point (copy it to add a model).\r\n\r\n## Create a block\r\n```\r\ncp -r _template <model-name>          # then edit block.json + src/\r\n```\r\n\r\nMinimum required files (what `_template/` contains):\r\n\r\n| Path | Role |\r\n|---|---|\r\n| `block.json` | manifest — the single source of truth (name, version, status, adapter, `deps`, `apple_native`, weights…) |\r\n| `README.md` | model card (task, inputs/outputs, license, Apple-native verdict) |\r\n| `cover.svg` | gallery cover |\r\n| `src/model.py` | real adapter — lazy heavy imports (torch/coreml); `is_available()` gates real vs mock |\r\n| `src/mock.py` | deterministic offline stand-in — no weights, runs in CI |\r\n| `src/metrics.py` | optional post-processing |\r\n| `src/__init__.py` | exposes `build(offline=False)` — the shared factory |\r\n| `tests/test_model.py` | offline, mock-backed, green in CI |\r\n| `swift/Model.swift` | on-device wrapper — only when promoted to `status: ready` |\r\n| `assets/` | optional sample I/O |\r\n\r\n## The contract every block satisfies\r\n```python\r\nfrom blocks.<name>.src import build\r\nmodel = build(offline=False)     # real backend if weights present, else the mock\r\nmodel.predict(inputs)            # one typed entry method\r\n```\r\nIdentical shape across blocks → an MCP server is a thin generic wrapper (`block.json` → tool\r\nschema → `predict`). See `ARCHITECTURE.md` §4.\r\n\r\nBlocks never import each other; related blocks share a **common core** (e.g. `vis_models.saliency`)\r\ndeclared in `block.json`'s `deps`. So each block is independently usable, and MCP-ifying one means\r\nserving the block **plus its `deps`** — see `ARCHITECTURE.md` §4b.\r\n\r\n## Testing\r\nBlocks are tested together as one package: `cd libraries/models/blocks && python -m pytest`\r\n(this dir is in `scripts/test-all.sh` and CI). The `pyproject.toml` here puts the block tree and\r\n`vis_models` on the path, so tests import `blocks.<name>.src.*`.\r\n\r\n## raw → ready\r\n`status: raw` = Python adapter + mock (default). Promote to `ready` (add `swift/`, build a CoreML\r\n`.mlpackage` under `../../ai-farm/ready/`) only when an app needs the model on device.\r\n",
    "assets": [],
    "examples": [],
    "dependencies": [],
    "related": [],
    "contentPath": "",
    "coverPath": "assets/library-card-cover.png",
    "coverPreviewPath": ""
  },
  {
    "slug": "boilerplate",
    "id": "boilerplate",
    "name": "Design project boilerplate",
    "type": "library",
    "category": "code-library",
    "description": "The starter kit every new app's design copies: - the lean component set (atoms → templates) it will actually use, - the template screens (onboarding, paywall, core, settings), - a `tokens.override.json` skeleton for the universe, - the design checklist (hierarchy, contrast, scannability, flow…). Spinning up a niche = **copy this → generate the universe** (`tokens.override.json` + moodboard via the purity tool) → apply. Keeps every app Apple-native and consistent from screen one.",
    "shortDescription": "Design project boilerplate",
    "keywords": [],
    "githubPath": "design-system/boilerplate",
    "readme": "# Design project boilerplate\r\n\r\nThe starter kit every new app's design copies:\r\n\r\n- the lean component set (atoms → templates) it will actually use,\r\n- the template screens (onboarding, paywall, core, settings),\r\n- a `tokens.override.json` skeleton for the universe,\r\n- the design checklist (hierarchy, contrast, scannability, flow…).\r\n\r\nSpinning up a niche = **copy this → generate the universe** (`tokens.override.json` +\r\nmoodboard via the purity tool) → apply. Keeps every app Apple-native and consistent from\r\nscreen one.\r\n",
    "assets": [],
    "examples": [],
    "dependencies": [],
    "related": [],
    "contentPath": "",
    "coverPath": "assets/library-card-cover.png",
    "coverPreviewPath": ""
  },
  {
    "slug": "code",
    "id": "code",
    "name": "Code library",
    "type": "library",
    "category": "code-library",
    "description": "Reusable Swift / Python code, packaged as **blocks** — one code unit per folder under `blocks/`, each self-describing (`block.json`), offline-tested, and MCP-ready. The full standard is in [`ARCHITECTURE.md`](ARCHITECTURE.md); `blocks/_template/` is the reference block to copy. **Rule:** if a sprint writes something twice, it becomes a block. New code is authored directly as a block; the discovery view is `catalog.json`.",
    "shortDescription": "Code library",
    "keywords": [],
    "version": "0.1.0",
    "githubPath": "libraries/code",
    "readme": "# Code library\r\n\r\nReusable Swift / Python code, packaged as **blocks** — one code unit per folder under `blocks/`,\r\neach self-describing (`block.json`), offline-tested, and MCP-ready. The full standard is in\r\n[`ARCHITECTURE.md`](ARCHITECTURE.md); `blocks/_template/` is the reference block to copy.\r\n\r\n**Rule:** if a sprint writes something twice, it becomes a block. New code is authored directly as\r\na block; the discovery view is `catalog.json`.\r\n\r\n## Blocks (`blocks/`)\r\n- `color-matching/` — seasonal color analysis (Spring/Summer/Autumn/Winter → palette + avoid) + color-wheel harmonies. Python.\r\n- `color-extraction/` — an image's dominant colors with coverage %. Python.\r\n- `vision-camera/` — on-device capture + CryptoKit image encryption. Swift.\r\n- `ux-position-mask/` — flag which screen elements sit in good/dangerous positions (thumb reach, active zones, safe areas). Python.\r\n- `ux-attention-pattern/` — classify a screen's reading flow (F / Z / focused / scattered) from its attention map and judge it. Python (uses the saliency attention modules).\r\n\r\n## Local UIs / tools (not blocks)\r\nSmall web UIs used to exercise blocks during development — no `block.json`, not MCP seams, some\r\ngitignored (local-only): `color-test-ui/`, `wireframe-studio/`, `face-parsing-ui/`, `vinet-ui/`.\r\n\r\n## Test\r\n```\r\ncd blocks && pytest        # all code blocks, offline\r\n```\r\n",
    "assets": [],
    "examples": [],
    "dependencies": [],
    "related": [],
    "contentPath": "",
    "coverPath": "assets/library-card-cover.png",
    "coverPreviewPath": ""
  },
  {
    "slug": "color_extraction",
    "id": "color_extraction",
    "name": "color-extraction",
    "type": "library",
    "category": "code",
    "description": "Upload an image and get its dominant colors as [{hex, percentage}] sorted by coverage. Near-identical colors are grouped by snapping each channel to a small grid, so the result is a handful of meaningful swatches rather than thousands of near-duplicates. The pure-Python core (extract_from_pixels) is dependency-free and tested; extract_colors() reads a file via Pillow (optional [image] extra). Feeds color-matching (the dominant/skin tone becomes the seasonal input).",
    "shortDescription": "Extract an image's dominant colors with their coverage percentages.",
    "keywords": [
      "dominant color extraction from an image"
    ],
    "version": "0.1.0",
    "githubPath": "libraries/code/blocks/color_extraction",
    "readme": "# Color extraction\r\n\r\n**What:** image → its dominant colors with coverage percentages.\r\n**In / out:** `image_path` → `[{\"hex\": \"#RRGGBB\", \"percentage\": <float>}, …]` (sorted desc).\r\n**First needed by:** Color Matching (Sprint 1) — the selfie's tones feed the palette match.\r\n\r\n```python\r\nfrom color_extraction import extract_colors, extract_from_pixels\r\n\r\nextract_colors(\"selfie.jpg\", n=8)          # top 8 colors + % (needs Pillow)\r\nextract_from_pixels(pixels, n=8)           # pure-Python core (tested, no deps)\r\n```\r\n\r\n- Near-identical colors are grouped (channel quantization) → a few meaningful swatches, not\r\n  thousands of near-duplicates. `n=None` returns every bucket.\r\n- Pure-Python core is dependency-free and tested; `extract_colors()` needs **Pillow**\r\n  (`pip install pillow`). CLI: `python color_extraction.py image.jpg [n]`.\r\n- Visual test: `../color-test-ui/index.html`.\r\n",
    "assets": [
      {
        "src": "cover.svg",
        "alt": "cover"
      }
    ],
    "examples": [],
    "dependencies": [],
    "related": [],
    "contentPath": "",
    "coverPath": "assets/library-card-cover.png",
    "coverPreviewPath": ""
  },
  {
    "slug": "color_matching",
    "id": "color_matching",
    "name": "color-matching",
    "type": "library",
    "category": "code",
    "description": "Two independent approaches. SEASONAL (primary): from a base color's undertone + value, classify the season and return the season's recommended palette and colors to avoid — the Color-Matching app's core (skin tone -> 'your colors'). HARMONIES (secondary): complementary, analogous, triadic, split-complementary, tetradic, and monochromatic schemes off a base color. Pure Python (stdlib colorsys), no dependencies.",
    "shortDescription": "Classify a color into a season (Spring/Summer/Autumn/Winter) with its palette and colors to avoid, plus classic harmonies.",
    "keywords": [
      "seasonal color analysis + color-wheel harmonies"
    ],
    "version": "0.1.0",
    "githubPath": "libraries/code/blocks/color_matching",
    "readme": "# Color matching\r\n\r\nTwo independent approaches, **seasonal first**.\r\n\r\n**1. Seasonal (primary)** — classify a color's undertone + value into a **season**, then **compute**\r\na recommended palette + colors to avoid. This is the Color-Matching app's core (skin tone → \"your\r\ncolors\"). See `libraries/knowledge/color-theory/` (`color-theory.md`, `seasons.md`).\r\n\r\n```python\r\nfrom color_matching import seasonal_match, seasonal_match_from_colors\r\n\r\nseasonal_match(\"#FF6600\")          # {season:\"Spring\", undertone, value, chroma, feel, palette[], avoid[]}\r\nseasonal_match_from_colors(extracted)   # pass color_extraction output; uses the dominant/skin tone\r\n```\r\n- **Classify** by undertone × value (from `seasons.md`): warm+light → **Spring**, warm+deep →\r\n  **Autumn**, cool+light → **Summer**, cool+deep → **Winter**.\r\n- **Compute the palette by rule** (per `color-rules.md` \"computed, not hand-picked\"): take harmonic\r\n  hues off the person's own color (base + analogous + complementary + triadic) and render each into\r\n  the **season's saturation/lightness band** (Spring = bright+light, Autumn = deep+muted, …), add a\r\n  rule-based neutral (+ black/white contrast for Winter). `avoid` = the same hues in the **opposite**\r\n  season's band. So the palette **varies per person** — it's not a static table.\r\n\r\n**2. Harmonies (secondary)** — classic color-wheel schemes off a base color.\r\n\r\n```python\r\nfrom color_matching import harmonies, complementary, triadic\r\nharmonies(\"#0E1B2A\")               # complementary / analogous / triadic / split / tetradic / mono\r\ncomplementary(\"#FF0000\")           # -> [\"#FF0000\", \"#00FFFF\"]\r\n```\r\n\r\n`match(hex)` returns **both**, seasonal on top. Pure Python (stdlib `colorsys`), no dependencies.\r\nCLI: `python color_matching.py \"#FF6600\"`. Visual test: `../color-test-ui/index.html`.\r\n",
    "assets": [
      {
        "src": "cover.svg",
        "alt": "cover"
      }
    ],
    "examples": [],
    "dependencies": [],
    "related": [],
    "contentPath": "",
    "coverPath": "assets/library-card-cover.png",
    "coverPreviewPath": ""
  },
  {
    "slug": "color-matching",
    "id": "color-matching",
    "name": "color-matching",
    "type": "application",
    "category": "ios-ready",
    "description": "See README.md for details.",
    "shortDescription": "Block for color-matching",
    "keywords": [
      "color matching"
    ],
    "version": "0.1.0",
    "githubPath": "libraries/ios-ready/blocks/color-matching",
    "readme": "# Color Matching (Sprint 1)\r\n\r\nSwiftUI starter. Consumes the shared layer (`design-system`, `agents`, `libraries`).\r\n\r\n## Run it\r\nThe `.xcodeproj` is not committed (it's generated/local). To build:\r\n1. Open Xcode → **File ▸ New ▸ Project ▸ iOS App**, name `ColorMatching`, bundle id `co.elktech.colormatching`.\r\n2. Add the files under `Sources/` to the target (or use XcodeGen / Swift Package).\r\n3. Run on a simulator. The flow follows `wireframe/` and the design-system templates.\r\n\r\n## Files\r\n- `Sources/ColorMatchingApp.swift` — entry point.\r\n- `Sources/ContentView.swift` — root flow.\r\n- `Sources/Onboarding/`, `ColorAnalysis/`, `OutfitScan/`, `Paywall/` — feature views.\r\n- `charter.md` — the spec. `universe/` — this app's tokens/moodboard.\r\n- `wireframe/`, `audit/` — agent outputs.\r\n\r\n## TODO (real implementation)\r\n- Replace `analyze()` with the `skin-tone-cv` CoreML model + `seasons.md` mapping.\r\n- Camera capture + encryption from `libraries/code/vision-camera`.\r\n- StoreKit 2 in the paywall.\r\n",
    "assets": [],
    "examples": [],
    "dependencies": [],
    "related": [],
    "contentPath": "",
    "coverPath": "assets/library-card-cover.png",
    "coverPreviewPath": ""
  },
  {
    "slug": "color-test-ui",
    "id": "color-test-ui",
    "name": "Color test UI",
    "type": "library",
    "category": "code",
    "description": "A self-contained `index.html` (no server) for visually testing the color code: - **Extract** — choose an image → see its dominant colors + percentages. - **Match** — pick a base color → see every harmony (complementary, analogous, triadic, split-complementary, tetradic, monochromatic) as swatches. Open `index.html` in any browser. The JS **mirrors** `color_extraction.py` / `color_matching.py` (same quantization + hue math) so it's a faithful visual check — the Python remains the source of truth for production.",
    "shortDescription": "Color test UI",
    "keywords": [],
    "githubPath": "libraries/code/color-test-ui",
    "readme": "# Color test UI\r\n\r\nA self-contained `index.html` (no server) for visually testing the color code:\r\n1. **Extract** — choose an image → see its dominant colors + percentages.\r\n2. **Match** — pick a base color → see every harmony (complementary, analogous, triadic,\r\n   split-complementary, tetradic, monochromatic) as swatches.\r\n\r\nOpen `index.html` in any browser. The JS **mirrors** `color_extraction.py` / `color_matching.py`\r\n(same quantization + hue math) so it's a faithful visual check — the Python remains the source of\r\ntruth for production.\r\n",
    "assets": [],
    "examples": [],
    "dependencies": [],
    "related": [],
    "contentPath": "",
    "coverPath": "assets/library-card-cover.png",
    "coverPreviewPath": ""
  },
  {
    "slug": "color-theory",
    "id": "color-theory",
    "name": "color-theory",
    "type": "library",
    "category": "knowledge",
    "description": "A lean-ai-factory block.",
    "shortDescription": "Block for color-theory",
    "keywords": [
      "color theory"
    ],
    "version": "0.1.0",
    "githubPath": "libraries/knowledge/blocks/color-theory",
    "assets": [],
    "examples": [],
    "dependencies": [],
    "related": [],
    "contentPath": "",
    "coverPath": "assets/library-card-cover.png",
    "coverPreviewPath": ""
  },
  {
    "slug": "composition",
    "id": "composition",
    "name": "composition",
    "type": "library",
    "category": "knowledge",
    "description": "A lean-ai-factory block.",
    "shortDescription": "Block for composition",
    "keywords": [
      "composition"
    ],
    "version": "0.1.0",
    "githubPath": "libraries/knowledge/blocks/composition",
    "assets": [],
    "examples": [],
    "dependencies": [],
    "related": [],
    "contentPath": "",
    "coverPath": "assets/library-card-cover.png",
    "coverPreviewPath": ""
  },
  {
    "slug": "design-system",
    "id": "design-system",
    "name": "Lean Design System",
    "type": "library",
    "category": "code-library",
    "description": "Apple-native component shell. **Built once, shared by every app.**",
    "shortDescription": "Lean Design System",
    "keywords": [],
    "version": "0.1.0",
    "githubPath": "design-system",
    "readme": "# Lean Design System\r\n\r\nApple-native component shell. **Built once, shared by every app.**\r\n\r\n## Two layers\r\n- **This folder** = the structure (`atoms` → `molecules` → `organisms` → `templates`). It carries **no** niche identity.\r\n- **Per-app identity** lives in `apps/<app>/universe/tokens.override.json` (colors, fonts, radius).\r\n\r\n## The one hard rule\r\nAgents may modify **tokens** only — never component code. Tokens are W3C DTCG\r\n(`$value` / `$type`). `tokens/build/` is generated by `tools/build_tokens.py` into SwiftUI\r\n(`.swift`) and CSS variables — never hand-edit `build/` (it's gitignored).\r\n\r\n## Build tokens\r\n```bash\r\n# resolve base + a per-app universe override -> SwiftUI + CSS\r\npython tools/build_tokens.py --override ../apps/color-matching/universe/tokens.override.json\r\n```\r\nDependency-free, validated (dangling/circular refs fail the build), and tested (`tests/`). This\r\nreplaces the Node \"Style Dictionary\" step — the tokens are real, consumable artifacts.\r\n\r\n## Source of truth\r\nFigma (see `FIGMA.md`) is the human source. It is **synced into** `tokens/` + the\r\ncomponent specs here; agents read **this repo**, never Figma live.\r\n\r\n## Adding a component\r\nCopy the nearest atom/molecule, keep it token-driven, document its tokens in its own\r\n`.md`. If Apple's UI Kit already has it, mirror their structure.\r\n\r\n## Structure\r\n```\r\nfoundations/   raw design decisions (color rules, type, spacing, motion, a11y, grids)\r\ntokens/        machine-readable DTCG tokens derived from foundations\r\natoms/ molecules/ organisms/ templates/   the component hierarchy\r\nplatforms/     per-surface variants: iphone ipad watch vision widgets dynamic-island notifications\r\nillustrations/ AI illustration prompts + assets\r\nsfsymbols/     SF Symbols selection + usage policy\r\npatterns/      Apple-DNA libraries: animation, continuous-experience, enhance-experience, interaction-ideas\r\nreferences/    curated competitor onboarding/paywall flows (view of libraries/memory)\r\nboilerplate/   the starter kit a new app's design copies\r\n```\r\nDesign **tools** live elsewhere: skills/agents in `agents/` (wireframing, ux-audit),\r\nMCP servers in `libraries/mcp/` (navigation-flow, wireframing, purity).\r\n\r\n",
    "assets": [],
    "examples": [],
    "dependencies": [],
    "related": [],
    "contentPath": "",
    "coverPath": "assets/library-card-cover.png",
    "coverPreviewPath": ""
  },
  {
    "slug": "face_parsing",
    "id": "face_parsing",
    "name": "face-parsing",
    "type": "model",
    "category": "models",
    "description": "BiSeNet (ResNet18/34) 19-class CelebAMask-HQ face parsing. Segments a face into parts (skin, eyes, lips, hair, ...) so the Colors Matching app can extract colors from each region. Real weights run in the AI-farm; a deterministic synthetic mask stands in for CI.",
    "shortDescription": "Per-pixel face-part masks from a selfie.",
    "keywords": [
      "facial segmentation (skin/eyes/lips/hair)"
    ],
    "version": "0.1.0",
    "githubPath": "libraries/models/blocks/face_parsing",
    "readme": "# face-parsing (model card)\r\n\r\nFacial segmentation — per-pixel face-part masks\r\n([yakhyo/face-parsing](https://github.com/yakhyo/face-parsing), BiSeNet / ResNet18–34).\r\n\r\n- **Task:** segment a selfie into 19 CelebAMask-HQ classes; grouped here into **skin / eyes / lips\r\n  / hair** — the regions the Colors Matching app extracts colors from.\r\n- **Inputs:** a face image (RGB). **Outputs:** a `SegMask` (grid of class ids) + `part_summary`\r\n  (present / coverage / bbox per part).\r\n- **Status:** `raw` — self-contained block (`deps: []`). Real `.pt`/`.onnx` weights run in the\r\n  AI-farm (`ai-farm/raw/face-parsing`); CI uses a deterministic synthetic mask.\r\n- **Apple-native:** *partial* — Apple has on-device **skin & hair** pixel mattes\r\n  (`AVSemanticSegmentationMatte`, iOS 13+) and eye/lip **landmarks** (Vision). See below.\r\n\r\n## Use\r\n```python\r\nfrom blocks.face_parsing.src import build\r\nfrom blocks.face_parsing.src.metrics import part_summary\r\n\r\nmodel = build()                         # real BiSeNet on the farm; synthetic mask elsewhere\r\nmask  = model.predict(\"selfie.png\")\r\npart_summary(mask)                      # {\"skin\": {...}, \"eyes\": {...}, \"lips\": {...}, \"hair\": {...}}\r\nmask.bbox([4, 5])                       # where the eyes are (normalized box)\r\n```\r\n\r\n## Apple-native alternatives (researched)\r\n\r\nChecked per the task requirement — Apple *partially* covers this on-device:\r\n\r\n| Apple API | Covers | Since | Gap vs face-parsing |\r\n|---|---|---|---|\r\n| AVFoundation `AVSemanticSegmentationMatte` (`.skin`, `.hair`, `.teeth`, `.glasses`) | per-pixel **skin & hair** mattes | iOS 13 | no eyes/lips class; produced at photo **capture** (`enabledSemanticSegmentationMatteTypes`), not on an arbitrary image |\r\n| Vision `VNDetectFaceLandmarksRequest` | eyes, lips, brows, nose, contour, pupils — as **points** | iOS 11 | geometric landmarks, not pixel masks |\r\n| Vision `VNGeneratePersonSegmentationRequest` / `…PersonInstanceMaskRequest` | whole **person** foreground | iOS 15 / 17 | not face parts |\r\n| ARKit `ARFaceAnchor` | 3D face mesh + blendshapes | iOS 11 | needs TrueDepth; no semantic part masks |\r\n\r\n**Verdict — partial.** For a captured selfie, Apple's native **skin + hair** mattes are a strong\r\non-device option and worth preferring for those parts. face-parsing is still needed for **eyes +\r\nlips** as pixel regions, a unified parse, and running on non-capture images. Recommendation for\r\nColors Matching: use Apple's native skin/hair mattes where available, face-parsing for eyes/lips\r\n(and as the cross-part fallback).\r\n\r\n## Running the real model (AI-farm)\r\nFetch the repo + a weight (`resnet18.pt`) into `ai-farm/raw/face-parsing/`, install `torch` +\r\n`opencv`, then `build()` returns the real `FaceParsingModel` (`is_available()` gates the fallback).\r\n",
    "assets": [
      {
        "src": "cover.svg",
        "alt": "cover"
      }
    ],
    "examples": [],
    "dependencies": [],
    "related": [],
    "contentPath": "",
    "coverPath": "assets/library-card-cover.png",
    "coverPreviewPath": ""
  },
  {
    "slug": "folder-test",
    "type": "folder",
    "category": "skills-library",
    "name": "Folder test",
    "shortDescription": "Folder content preview",
    "description": "Test article using a content folder instead of a single PDF or Markdown file.",
    "keywords": [
      "folder",
      "test"
    ],
    "contentPath": "content/articles/folder-test/content",
    "coverPath": "content/articles/folder-test/cover.png",
    "coverPreviewPath": ""
  },
  {
    "slug": "foundations",
    "id": "foundations",
    "name": "Foundations",
    "type": "library",
    "category": "code-library",
    "description": "The lowest design layer — the raw decisions the tokens are derived from: color, typography, spacing, radius, elevation, motion, accessibility, breakpoints, grids. Foundations are *expressed* as machine-readable tokens in `../tokens/` (W3C DTCG); this folder documents the **rationale and rules** behind them. Humans set foundations here; agents only ever touch the derived tokens. - `color-rules.md` — how palettes are computed (not hand-picked); harmonies, 60-30-10, WCAG. - `typography.md` — the four type roles and the hierarchy rules. - `spacing.md` — the 4-pt spacing scale, radius, and elevation. - `accessibility.md` — contrast gate, hit targets, Dynamic Type, VoiceOver. - `materials.md` — Liquid Glass translucency (the `material.*` tokens). - `motion.md` — animation durations + easing (the `motion.*` tokens). - `breakpoints.md` — size classes + width thresholds (the `breakpoint.*` tokens). - `grids.md` — margins, gutters, columns, readable width.",
    "shortDescription": "Foundations",
    "keywords": [],
    "githubPath": "design-system/foundations",
    "readme": "# Foundations\r\n\r\nThe lowest design layer — the raw decisions the tokens are derived from: color,\r\ntypography, spacing, radius, elevation, motion, accessibility, breakpoints, grids.\r\n\r\nFoundations are *expressed* as machine-readable tokens in `../tokens/` (W3C DTCG);\r\nthis folder documents the **rationale and rules** behind them. Humans set foundations\r\nhere; agents only ever touch the derived tokens.\r\n\r\n- `color-rules.md` — how palettes are computed (not hand-picked); harmonies, 60-30-10, WCAG.\r\n- `typography.md` — the four type roles and the hierarchy rules.\r\n- `spacing.md` — the 4-pt spacing scale, radius, and elevation.\r\n- `accessibility.md` — contrast gate, hit targets, Dynamic Type, VoiceOver.\r\n- `materials.md` — Liquid Glass translucency (the `material.*` tokens).\r\n- `motion.md` — animation durations + easing (the `motion.*` tokens).\r\n- `breakpoints.md` — size classes + width thresholds (the `breakpoint.*` tokens).\r\n- `grids.md` — margins, gutters, columns, readable width.\r\n\r\nColor theory behind `color-rules.md` lives in the knowledge corpus:\r\n`../../libraries/knowledge/color-theory/color-theory.md`.\r\n",
    "assets": [],
    "examples": [],
    "dependencies": [],
    "related": [],
    "contentPath": "",
    "coverPath": "assets/library-card-cover.png",
    "coverPreviewPath": ""
  },
  {
    "slug": "illustrations",
    "id": "illustrations",
    "name": "Illustrations (AD & prompts)",
    "type": "library",
    "category": "code-library",
    "description": "The AI-generated illustration system: a **prompt library + style rules + generated assets**. Each entry pairs a prompt (with the model + settings used) to its rendered asset, so a niche's illustrations stay on-brand and reproducible. Style is driven by the per-app **universe** (palette / mood from the purity tool). Version prompts next to their outputs.",
    "shortDescription": "Illustrations (AD & prompts)",
    "keywords": [],
    "githubPath": "design-system/illustrations",
    "readme": "# Illustrations (AD & prompts)\r\n\r\nThe AI-generated illustration system: a **prompt library + style rules + generated assets**.\r\n\r\nEach entry pairs a prompt (with the model + settings used) to its rendered asset, so a\r\nniche's illustrations stay on-brand and reproducible. Style is driven by the per-app\r\n**universe** (palette / mood from the purity tool). Version prompts next to their outputs.\r\n",
    "assets": [],
    "examples": [],
    "dependencies": [],
    "related": [],
    "contentPath": "",
    "coverPath": "assets/library-card-cover.png",
    "coverPreviewPath": ""
  },
  {
    "slug": "iris",
    "id": "iris",
    "name": "iris",
    "type": "model",
    "category": "models",
    "description": "MediaPipe Iris (PyTorch port) locates the pupil center + iris boundary and eye contour from a 64x64 eye crop. Used by Colors Matching to pull the person's eye colour from the precise iris disc (face-parsing gives the rough eye region; this pinpoints the iris). Weights are a one-time TFLite->PyTorch conversion (torch-only at runtime, CPU-fast); the upstream MediaPipe face-mesh crop stage is skipped in favour of a supplied eye box.",
    "shortDescription": "Locates the iris in an eye, so the eye colour can be sampled.",
    "keywords": [
      "iris / eye-landmark detection (pupil center + iris boundary)"
    ],
    "version": "0.1.0",
    "githubPath": "libraries/models/blocks/iris",
    "readme": "# iris (model card)\r\n\r\nIris / eye-landmark detection — pupil center + iris boundary\r\n([Morris88826/MediaPipe_Iris](https://github.com/Morris88826/MediaPipe_Iris), a PyTorch port of\r\nGoogle's MediaPipe Iris).\r\n\r\n- **Task:** locate the **iris** (pupil center + boundary) and eye contour in an eye.\r\n- **Inputs:** an **eye crop**, or a **face image + a normalized eye box** `(x,y,w,h)` — the box comes\r\n  from **face-parsing** (Task 4), so the two compose: face-parsing finds the eye, iris pinpoints it.\r\n- **Outputs:** an `IrisResult` (center, radius, boundary points) + `metrics.eye_color` — the\r\n  person's **eye colour**, sampled from the iris ring (the Colors Matching payload).\r\n- **Status:** `raw`, self-contained (`deps: []`). Weights are a one-time TFLite→PyTorch conversion\r\n  (`iris_landmark.pth`, ~2.7MB); after that it's **torch-only** and CPU-fast. The upstream MediaPipe\r\n  face-mesh crop stage is skipped (we supply the eye box), so **no mediapipe/tensorflow at runtime**.\r\n- **Apple-native:** *partial* — Vision gives single **pupil points**, ARKit gives gaze/eye\r\n  transforms (TrueDepth), but neither localizes the **iris disc** on an arbitrary photo.\r\n\r\n## Use\r\n```python\r\nfrom blocks.iris.src import build\r\nfrom blocks.iris.src.metrics import eye_color\r\n\r\nmodel = build()                                    # real model when weights are on the box\r\nres   = model.predict(\"selfie.jpg\", eye_box=(0.36, 0.34, 0.10, 0.06))   # box from face-parsing\r\nres.center, res.radius                             # where the iris is (normalized in the crop)\r\neye_color(\"selfie.jpg\", res)                       # -> {\"rgb\": [...], \"hex\": \"#..\"} eye colour\r\n```\r\n\r\n## Producing the weights (one-time)\r\nThe repo ships `data/iris_landmark.tflite`; convert it to `iris_landmark.pth` (read the TFLite with\r\n`ai-edge-litert` — no full TensorFlow needed), then place it under `ai-farm/raw/iris/`. Done once on\r\nthe farm; the block auto-discovers `libs/iris.py` + the `.pth` and runs torch-only.\r\n",
    "assets": [
      {
        "src": "cover.svg",
        "alt": "cover"
      }
    ],
    "examples": [],
    "dependencies": [],
    "related": [],
    "contentPath": "",
    "coverPath": "assets/library-card-cover.png",
    "coverPreviewPath": ""
  },
  {
    "slug": "knowledge",
    "id": "knowledge",
    "name": "Knowledge / memory modules",
    "type": "library",
    "category": "code-library",
    "description": "Per-vertical corpora the apps and agents draw on. One folder per domain; each file names its source and how it's used in its own header. Seeded corpora: - `color-theory/` - `color-theory.md` — full color reference (properties, harmonies, the 7-step palette process,   60-30-10, accessibility, color meanings, cultural cautions). Source: `Color Theory.pdf`. - `seasons.md` — the seasonal-analysis subset used by the Color Matching app. - `wireframing/` - `wireframing-theory.md` — full wireframing theory. Source: `Wireframing.pdf`. - `wireframing.md` — curated flow-rules / iOS-pattern cheat-sheet. - `composition/` - `composition-theory.md` — full composition reference (composition types, elements and their   properties, contrast, similarity, repetition, rhythm, balance, hierarchy, grids, Gestalt,   focal points) applied to UI/screen design. Source: `Design Composition.pdf`. - `AppleComposition-guidelines.md` — full transcript of Apple's WWDC25 \"Get to know the new   design system\" session (Liquid Glass, concentricity, structure, continuity).   Source: youtube.com/watch?v=DS2ildqCrB0. - `apple-guidelines/` - `app-store-review-guidelines.md` — the full App Store Review Guidelines (all five sections).   Source: developer.apple.com/app-store/review/guidelines/.",
    "shortDescription": "Knowledge / memory modules",
    "keywords": [],
    "githubPath": "libraries/knowledge",
    "readme": "# Knowledge / memory modules\r\n\r\nPer-vertical corpora the apps and agents draw on. One folder per domain; each file names its\r\nsource and how it's used in its own header.\r\n\r\nSeeded corpora:\r\n- `color-theory/`\r\n  - `color-theory.md` — full color reference (properties, harmonies, the 7-step palette process,\r\n    60-30-10, accessibility, color meanings, cultural cautions). Source: `Color Theory.pdf`.\r\n  - `seasons.md` — the seasonal-analysis subset used by the Color Matching app.\r\n- `wireframing/`\r\n  - `wireframing-theory.md` — full wireframing theory. Source: `Wireframing.pdf`.\r\n  - `wireframing.md` — curated flow-rules / iOS-pattern cheat-sheet.\r\n- `composition/`\r\n  - `composition-theory.md` — full composition reference (composition types, elements and their\r\n    properties, contrast, similarity, repetition, rhythm, balance, hierarchy, grids, Gestalt,\r\n    focal points) applied to UI/screen design. Source: `Design Composition.pdf`.\r\n  - `AppleComposition-guidelines.md` — full transcript of Apple's WWDC25 \"Get to know the new\r\n    design system\" session (Liquid Glass, concentricity, structure, continuity).\r\n    Source: youtube.com/watch?v=DS2ildqCrB0.\r\n- `apple-guidelines/`\r\n  - `app-store-review-guidelines.md` — the full App Store Review Guidelines (all five sections).\r\n    Source: developer.apple.com/app-store/review/guidelines/.\r\n\r\nAs verticals grow we merge and reuse rather than re-collecting.\r\n",
    "assets": [],
    "examples": [],
    "dependencies": [],
    "related": [],
    "contentPath": "",
    "coverPath": "assets/library-card-cover.png",
    "coverPreviewPath": ""
  },
  {
    "slug": "legal",
    "id": "legal",
    "name": "legal",
    "type": "library",
    "category": "code-library",
    "description": "Generates a **Privacy Policy** + **Terms & Conditions** for an app from its data-use profile. Deterministic template filling — **not legal advice**. Every document is stamped *\"requires human/legal review before publishing\"* and `generate()` returns `review_required=True`. Why it exists: the App Store **requires a privacy policy** (Guideline 5.1). The compliance agent only *checks* for one; this produces a truthful first draft grounded in what the app actually does — especially **on-device** processing (no server → strong, honest privacy stance). Placeholders left as `[COMPANY]`, `[JURISDICTION]`, etc. are surfaced in `warnings` so nothing ships half-filled. **A lawyer must review the output before it goes live.**",
    "shortDescription": "legal",
    "keywords": [],
    "version": "0.1.0",
    "githubPath": "libraries/legal",
    "readme": "# legal\r\n\r\nGenerates a **Privacy Policy** + **Terms & Conditions** for an app from its data-use profile.\r\nDeterministic template filling — **not legal advice**. Every document is stamped *\"requires\r\nhuman/legal review before publishing\"* and `generate()` returns `review_required=True`.\r\n\r\nWhy it exists: the App Store **requires a privacy policy** (Guideline 5.1). The compliance agent\r\nonly *checks* for one; this produces a truthful first draft grounded in what the app actually does\r\n— especially **on-device** processing (no server → strong, honest privacy stance).\r\n\r\n```python\r\nfrom legal import AppLegalProfile, write_legal_docs\r\n\r\nprofile = AppLegalProfile(\r\n    app_name=\"Color Matching\", company=\"EIK Tech\", contact_email=\"privacy@eiktech.co\",\r\n    collects_data=[\"a selfie you take (processed on-device)\"], on_device=True,\r\n    uses_iap=True, region=\"France\", effective_date=\"2026-07-02\",\r\n)\r\nres = write_legal_docs(profile, \"apps/color-matching/legal\")\r\nres[\"review_required\"]   # True — always\r\nres[\"warnings\"]          # e.g. unfilled placeholders + the mandatory lawyer-review note\r\n```\r\n\r\nPlaceholders left as `[COMPANY]`, `[JURISDICTION]`, etc. are surfaced in `warnings` so nothing\r\nships half-filled. **A lawyer must review the output before it goes live.**\r\n",
    "assets": [],
    "examples": [],
    "dependencies": [],
    "related": [],
    "contentPath": "",
    "coverPath": "assets/library-card-cover.png",
    "coverPreviewPath": ""
  },
  {
    "slug": "mcp",
    "id": "mcp",
    "name": "MCP library",
    "type": "library",
    "category": "code-library",
    "description": "Shared MCP servers/tools the agents call — e.g. the wireframing flow-audit server and the Figma sync server. One folder per server, each with its own README and run instructions. Keep auth/config out of the repo; document where it's injected.",
    "shortDescription": "MCP library",
    "keywords": [],
    "githubPath": "libraries/mcp",
    "readme": "# MCP library\r\n\r\nShared MCP servers/tools the agents call — e.g. the wireframing flow-audit server and\r\nthe Figma sync server. One folder per server, each with its own README and run\r\ninstructions. Keep auth/config out of the repo; document where it's injected.\r\n",
    "assets": [],
    "examples": [],
    "dependencies": [],
    "related": [],
    "contentPath": "",
    "coverPath": "assets/library-card-cover.png",
    "coverPreviewPath": ""
  },
  {
    "slug": "memory",
    "id": "memory",
    "name": "App-Benchmark memory",
    "type": "library",
    "category": "code-library",
    "description": "A queryable store of competitor **screens, flows, onboarding, and paywalls** — the \"memory app\" from the deck. - **Sources:** screensdesign.com (screens/flows), Sensor Tower (revenue), startups.rip (what failed). - `scripts/ingest.py` pulls and tags each screen by app / niche / screen-type into `benchmark.db`. - **Consumers:** the wireframing agent (best-practice flows) and feature decomposition (minimal feature set per niche). Phase 1: ingest the Color Matching competitor + 2–3 onboarding/paywall references under `sources/color-matching/`.",
    "shortDescription": "App-Benchmark memory",
    "keywords": [],
    "githubPath": "libraries/memory",
    "readme": "# App-Benchmark memory\r\n\r\nA queryable store of competitor **screens, flows, onboarding, and paywalls** — the\r\n\"memory app\" from the deck.\r\n\r\n- **Sources:** screensdesign.com (screens/flows), Sensor Tower (revenue), startups.rip (what failed).\r\n- `scripts/ingest.py` pulls and tags each screen by app / niche / screen-type into `benchmark.db`.\r\n- **Consumers:** the wireframing agent (best-practice flows) and feature decomposition (minimal feature set per niche).\r\n\r\nPhase 1: ingest the Color Matching competitor + 2–3 onboarding/paywall references under\r\n`sources/color-matching/`.\r\n",
    "assets": [],
    "examples": [],
    "dependencies": [],
    "related": [],
    "contentPath": "",
    "coverPath": "assets/library-card-cover.png",
    "coverPreviewPath": ""
  },
  {
    "slug": "microcopy",
    "id": "microcopy",
    "name": "microcopy",
    "type": "agent",
    "category": "agents",
    "description": "See SKILL.md for details.",
    "shortDescription": "Block for microcopy",
    "keywords": [
      "microcopy"
    ],
    "version": "0.1.0",
    "githubPath": "libraries/agents/blocks/microcopy",
    "skill": "# Microcopy agent\r\n\r\nStep 4 of the design-automation pipeline (deck p.60): the words. LLM writes concise, benefit-led\r\ncopy per screen slot; a deterministic checker enforces the HIG rules (see `references/voice-and-tone.md`)\r\nand re-asks once on failure.\r\n\r\n```python\r\nfrom microcopy import MicrocopyAgent\r\nres = MicrocopyAgent(ctx).run({\r\n    \"spec\": wireframe_result[\"spec\"],     # {screens: [...]} from the wireframing agent\r\n    \"persona\": \"…\", \"voice\": \"warm, clear\",\r\n    \"app\": \"ColorMatching\",\r\n})\r\nres.output[\"copy\"]         # {screen_id: {title, elements{}, actions{}, links{}}} -> feed swiftui-gen\r\nres.output[\"all_passed\"]   # gate\r\n```\r\n\r\n**Rules enforced:** title ≤ 40 chars; button labels 1–4 words, ≤ 22 chars, no trailing period,\r\nnever ALL CAPS; every wireframe element gets copy. Generation is **per screen** so each can be\r\nreviewed and adjusted independently.\r\n",
    "assets": [],
    "examples": [],
    "dependencies": [
      "pydantic>=2.6"
    ],
    "related": [],
    "contentPath": "",
    "coverPath": "assets/library-card-cover.png",
    "coverPreviewPath": ""
  },
  {
    "slug": "ml-core",
    "id": "ml-core",
    "name": "ml-core",
    "type": "library",
    "category": "code-library",
    "description": "The factory's **shared LLM layer** — one tested, provider-agnostic gateway + cost meter + tracking that every agent and tool depends on. Extracted from the skill-optimizer so there's a single, hardened seam for all LLM calls (no per-tool reinvention).",
    "shortDescription": "ml-core",
    "keywords": [],
    "version": "0.1.0",
    "githubPath": "libraries/ml-core",
    "readme": "# ml-core\r\n\r\nThe factory's **shared LLM layer** — one tested, provider-agnostic gateway + cost meter +\r\ntracking that every agent and tool depends on. Extracted from the skill-optimizer so there's\r\na single, hardened seam for all LLM calls (no per-tool reinvention).\r\n\r\n## What it gives you\r\n- **`build_gateway(cfg, meter, offline=…)`** → `gateway.complete(alias, messages)` across all\r\n  providers (Claude / Codex / Grok / Kimi / DeepSeek), with retries/backoff and param-dropping.\r\n- **`CostMeter`** — tokens / USD / latency per call, per model, with a hard budget cap.\r\n- **`build_tracker(...)`** — MLflow wrapper (`log_params/metrics/text`), degrades to a no-op\r\n  if MLflow isn't installed.\r\n- **`load_env()`** — pulls provider keys from `.env`.\r\n- **`MockGateway`** — deterministic offline backend (no keys, no spend) for tests/CI.\r\n\r\n## Use it\r\n```python\r\nfrom ml_core import load_env, load_providers, CostMeter, build_gateway\r\n\r\nload_env()                                   # reads .env (provider keys)\r\ncfg   = load_providers(\"providers.yaml\")\r\nmeter = CostMeter(budget_usd=5.0)\r\ngw    = build_gateway(cfg, meter, offline=False)   # offline=True -> MockGateway, no keys\r\n\r\nresp = gw.complete(\"deepseek\", [{\"role\": \"user\", \"content\": \"hello\"}])\r\nprint(resp.text, meter.summary())\r\n```\r\n\r\n## Design notes\r\n- **Decoupled:** ml-core knows nothing about skills/agents — just LLM plumbing, cost, config,\r\n  tracking. Agents import it; it imports nothing of theirs.\r\n- **Lazy deps:** `litellm` and `mlflow` are imported only when actually used, so `import ml_core`\r\n  and the whole offline/mock path need only `pydantic` + `pyyaml`.\r\n- **Pools** (`smart`/`fast`/`vision` in `providers.yaml`) are caller-side groupings; the gateway\r\n  dispatches a single alias — pool selection is the caller's job.\r\n\r\n## Test (offline, no keys)\r\n```bash\r\ncd libraries/ml-core\r\npython -m pytest          # uses the mock gateway; no providers, no spend\r\n```\r\n",
    "assets": [],
    "examples": [],
    "dependencies": [
      "pydantic>=2.6",
      "pyyaml>=6.0",
      "python-dotenv>=1.0"
    ],
    "related": [],
    "contentPath": "",
    "coverPath": "assets/library-card-cover.png",
    "coverPreviewPath": ""
  },
  {
    "slug": "models",
    "id": "models",
    "name": "Models library (`vis_models`)",
    "type": "library",
    "category": "code-library",
    "description": "The factory's **vision-model layer**. Two parts: - **`catalog.json`** — a human-readable discovery view over the AI-farm registry: model name, task, status, license, and the niche(s) it serves. It stores **no weights** (those live in `../ai-farm`). - **`src/vis_models/`** — the importable adapters Sprint Engineers and agents call. Today this is `saliency`.",
    "shortDescription": "Models library (`vis_models`)",
    "keywords": [],
    "version": "0.1.0",
    "githubPath": "libraries/models",
    "readme": "# Models library (`vis_models`)\r\n\r\nThe factory's **vision-model layer**. Two parts:\r\n\r\n- **`catalog.json`** — a human-readable discovery view over the AI-farm registry: model name,\r\n  task, status, license, and the niche(s) it serves. It stores **no weights** (those live in\r\n  `../ai-farm`).\r\n- **`src/vis_models/`** — the importable adapters Sprint Engineers and agents call. Today this is\r\n  `saliency`.\r\n\r\n## Saliency — \"does attention land on the primary action?\"\r\n\r\n`vis_models.saliency` predicts a human-attention heatmap for a screen and checks whether the\r\npredicted gaze peaks on the primary call-to-action. The UX-audit agent consumes it.\r\n\r\n**Backend: SUM** ([Arhosseini77/SUM](https://github.com/Arhosseini77/SUM)) — *Saliency Unification\r\nthrough Mamba*. Chosen over [ViNet](https://github.com/samyak0210/ViNet) because the audit scores\r\n**still screens**, and SUM is an image model with a UI/web-page condition tuned for interface\r\nimagery. ViNet is video-only and stays deferred for onboarding clips.\r\n\r\n### The seam (mirrors `ml_core`'s gateway)\r\n| Piece | Role |\r\n|---|---|\r\n| `SaliencyModel` (Protocol) | `predict(image) -> SaliencyMap` |\r\n| `SaliencyMap` | a small normalized grid (not a pixel array) — keeps metrics dependency-free |\r\n| `MockSaliencyModel` | deterministic, offline, **no torch/weights** — used in CI |\r\n| `SUMSaliencyModel` | real SUM; lazy `torch` import; runs in the **AI Farm** |\r\n| `analyze(map, cta_region)` | → `SaliencyReport` (focal point, dispersion, CTA attention, pass/fail) |\r\n| `build_saliency(backend, offline=...)` | picks SUM if available, else transparently the mock |\r\n\r\n```python\r\nfrom vis_models.saliency import analyze, build_saliency, Region\r\n\r\nsal = build_saliency(\"sum\")                       # real SUM in the farm; mock fallback elsewhere\r\nreport = analyze(sal.predict(\"screen.png\"),\r\n                 Region(0.25, 0.80, 0.50, 0.12))   # primary button, normalized coords\r\nreport.passes            # True if the CTA captures >= threshold of predicted attention\r\n```\r\n\r\n### Running the real SUM (AI Farm)\r\n1. Fetch the SUM repo + checkpoint into `../ai-farm/raw/SUM/` (per `ai-farm/registry.json`).\r\n2. `pip install -e \".[sum]\"` (pulls `torch`, `numpy`).\r\n3. `build_saliency(\"sum\")` now returns the real model; `is_available()` gates the fallback.\r\n\r\nCore (`mock` + metrics) is **dependency-free and tested offline**; only the SUM backend needs\r\nthe heavy extras, so CI stays fast and green without weights.\r\n",
    "assets": [],
    "examples": [],
    "dependencies": [],
    "related": [],
    "contentPath": "",
    "coverPath": "assets/library-card-cover.png",
    "coverPreviewPath": ""
  },
  {
    "slug": "navigation-flow",
    "id": "navigation-flow",
    "name": "navigation-flow",
    "type": "library",
    "category": "mcp",
    "description": "See README.md for details.",
    "shortDescription": "Block for navigation-flow",
    "keywords": [
      "navigation flow"
    ],
    "version": "0.1.0",
    "githubPath": "libraries/mcp/blocks/navigation-flow",
    "readme": "# Navigation / Flow Analysis MCP\r\n\r\nAn MCP server that analyzes a screen flow: builds the navigation graph and computes\r\nclicks-to-action, navigation depth, orphan pages / dead ends, and features-per-page.\r\n\r\nThe wireframing agent calls it to grade a flow (the \"A score\" loop). The deterministic\r\nlogic mirrors `agents/wireframing/scripts/flow_audit.py`; exposing it as an MCP lets any\r\nagent (Codex / Claude) call it directly.\r\n\r\n_Server implementation: TODO._\r\n",
    "assets": [],
    "examples": [],
    "dependencies": [],
    "related": [],
    "contentPath": "",
    "coverPath": "assets/library-card-cover.png",
    "coverPreviewPath": ""
  },
  {
    "slug": "patterns",
    "id": "patterns",
    "name": "Patterns (Apple-DNA libraries)",
    "type": "library",
    "category": "code-library",
    "description": "Reusable interaction / experience patterns drawn from Apple's design DNA. One subfolder per library: Each entry documents **what it is, when to use it, and the components/tokens it touches**. These feed the prototype + universe-enhancing design steps.",
    "shortDescription": "Patterns (Apple-DNA libraries)",
    "keywords": [],
    "githubPath": "design-system/patterns",
    "readme": "# Patterns (Apple-DNA libraries)\r\n\r\nReusable interaction / experience patterns drawn from Apple's design DNA. One subfolder\r\nper library:\r\n\r\n| Library | What it holds |\r\n|---|---|\r\n| `animation/` | motion patterns — transitions, springs, scroll-driven effects |\r\n| `continuous-experience/` | cross-device continuity — Handoff, Universal Clipboard, calls-into-app |\r\n| `enhance-experience/` | system integrations that elevate UX — Live Activities, App Intents, widgets, Siri |\r\n| `interaction-ideas/` | a catalog of interaction concepts to pull from per niche |\r\n\r\nEach entry documents **what it is, when to use it, and the components/tokens it touches**.\r\nThese feed the prototype + universe-enhancing design steps.\r\n",
    "assets": [],
    "examples": [],
    "dependencies": [],
    "related": [],
    "contentPath": "",
    "coverPath": "assets/library-card-cover.png",
    "coverPreviewPath": ""
  },
  {
    "slug": "platforms",
    "id": "platforms",
    "name": "Platforms",
    "type": "library",
    "category": "code-library",
    "description": "The design system covers **every Apple surface**, not just the phone screen. Each subfolder holds the platform-specific component variants + Figma coverage notes: Components stay **token-driven**; these folders capture how an organism/template *adapts* per surface. Most niches use a subset — populate per app, don't pre-build all.",
    "shortDescription": "Platforms",
    "keywords": [],
    "githubPath": "design-system/platforms",
    "readme": "# Platforms\r\n\r\nThe design system covers **every Apple surface**, not just the phone screen. Each\r\nsubfolder holds the platform-specific component variants + Figma coverage notes:\r\n\r\n| Folder | Surface |\r\n|---|---|\r\n| `iphone/` | primary app UI |\r\n| `ipad/` | adaptive / multi-column layouts |\r\n| `watch/` | complications, short glances |\r\n| `vision/` | spatial UI (visionOS) |\r\n| `widgets/` | Home / Lock Screen widgets |\r\n| `dynamic-island/` | Live Activities presentations |\r\n| `notifications/` | rich notification layouts |\r\n\r\nComponents stay **token-driven**; these folders capture how an organism/template\r\n*adapts* per surface. Most niches use a subset — populate per app, don't pre-build all.\r\n",
    "assets": [],
    "examples": [],
    "dependencies": [],
    "related": [],
    "contentPath": "",
    "coverPath": "assets/library-card-cover.png",
    "coverPreviewPath": ""
  },
  {
    "slug": "purity",
    "id": "purity",
    "name": "purity",
    "type": "library",
    "category": "mcp",
    "description": "See README.md for details.",
    "shortDescription": "Block for purity",
    "keywords": [
      "purity"
    ],
    "version": "0.1.0",
    "githubPath": "libraries/mcp/blocks/purity",
    "readme": "# Purity MCP\r\n\r\nWraps the moodboard **purity** tool (`cog-ai-intheshell/moodboard-purity-1`): given a\r\nbundle of persona images, it extracts the aesthetic — palette, tags, emotions — and\r\nscores moodboard coherence (\"purity\").\r\n\r\nOutput feeds the per-app **universe** (`apps/<app>/universe/tokens.override.json`) and the\r\nillustration style. This is the engine behind the \"Lean Creative Universe.\"\r\n\r\n_Server wraps the existing tool; implementation: TODO._\r\n",
    "assets": [],
    "examples": [],
    "dependencies": [],
    "related": [],
    "contentPath": "",
    "coverPath": "assets/library-card-cover.png",
    "coverPreviewPath": ""
  },
  {
    "slug": "rag",
    "id": "rag",
    "name": "rag",
    "type": "library",
    "category": "code-library",
    "description": "A small, **dependency-free** retrieval library for grounding agents in a document corpus. - **`Corpus`** — documents chunked into pieces, each carrying its `source` (for citations). - **`load_corpus(dir)`** — index every `*.md` (uses a `source:` line as the citation URL). - **`LexicalRetriever`** — TF-IDF cosine over chunks. No external deps; a solid baseline for a focused doc set and fully offline-testable. - **`Retriever`** protocol — the seam for a future `EmbeddingRetriever` (semantic recall via the ml-core providers) that drops in without changing callers. Used by `agents/apple-docs-rag` to answer Apple-development questions with citations.",
    "shortDescription": "rag",
    "keywords": [],
    "version": "0.1.0",
    "githubPath": "libraries/rag",
    "readme": "# rag\r\n\r\nA small, **dependency-free** retrieval library for grounding agents in a document corpus.\r\n\r\n- **`Corpus`** — documents chunked into pieces, each carrying its `source` (for citations).\r\n- **`load_corpus(dir)`** — index every `*.md` (uses a `source:` line as the citation URL).\r\n- **`LexicalRetriever`** — TF-IDF cosine over chunks. No external deps; a solid baseline for a\r\n  focused doc set and fully offline-testable.\r\n- **`Retriever`** protocol — the seam for a future `EmbeddingRetriever` (semantic recall via the\r\n  ml-core providers) that drops in without changing callers.\r\n\r\n```python\r\nfrom rag import load_corpus, LexicalRetriever\r\nr = LexicalRetriever(load_corpus(\"path/to/docs\"))\r\nfor chunk, score in r.search(\"how does Guideline 4.3 treat variant apps?\", k=4):\r\n    print(score, chunk.title, chunk.source)\r\n```\r\n\r\nUsed by `agents/apple-docs-rag` to answer Apple-development questions with citations.\r\n",
    "assets": [],
    "examples": [],
    "dependencies": [],
    "related": [],
    "contentPath": "",
    "coverPath": "assets/library-card-cover.png",
    "coverPreviewPath": ""
  },
  {
    "slug": "references",
    "id": "references",
    "name": "References (from competition)",
    "type": "library",
    "category": "code-library",
    "description": "Curated, **design-facing** views of competitor best-practice flows — the onboarding and paywall sequences worth copying (the \"97% familiar\" part of the strategy). - `onboarding/` — best-in-class onboarding flows. - `paywall/` — best-in-class paywall layouts. **Source of truth is `libraries/memory`** (the App-Benchmark DB). This folder holds the *curated selections + design notes* — which patterns to reuse and why — **not** the raw captures. Pull screens from `libraries/memory`; annotate the picks here. Don't duplicate the captures.",
    "shortDescription": "References (from competition)",
    "keywords": [],
    "githubPath": "design-system/references",
    "readme": "# References (from competition)\r\n\r\nCurated, **design-facing** views of competitor best-practice flows — the onboarding and\r\npaywall sequences worth copying (the \"97% familiar\" part of the strategy).\r\n\r\n- `onboarding/` — best-in-class onboarding flows.\r\n- `paywall/` — best-in-class paywall layouts.\r\n\r\n**Source of truth is `libraries/memory`** (the App-Benchmark DB). This folder holds the\r\n*curated selections + design notes* — which patterns to reuse and why — **not** the raw\r\ncaptures. Pull screens from `libraries/memory`; annotate the picks here. Don't duplicate\r\nthe captures.\r\n",
    "assets": [],
    "examples": [],
    "dependencies": [],
    "related": [],
    "contentPath": "",
    "coverPath": "assets/library-card-cover.png",
    "coverPreviewPath": ""
  },
  {
    "slug": "render",
    "id": "render",
    "name": "rendering",
    "type": "library",
    "category": "code-library",
    "description": "Turns a screen into an **image + the primary-action region**, so the UX-audit's SUM saliency check can run on a *real* screen instead of a screenshot you supply by hand. Mirrors the `vis_models` saliency seam: **protocol + real backend + offline mock + factory**. Wireframes from the wireframing agent use a known structure (`button.action` / `a.nav`), so the CTA box is reliable offline. **Real PNGs** need `pip install \"rendering[browser]\"` + `playwright install chromium` (dev/farm); CI stays on the dependency-free mock.",
    "shortDescription": "rendering",
    "keywords": [],
    "version": "0.1.0",
    "githubPath": "libraries/render",
    "readme": "# rendering\r\n\r\nTurns a screen into an **image + the primary-action region**, so the UX-audit's SUM saliency\r\ncheck can run on a *real* screen instead of a screenshot you supply by hand.\r\n\r\nMirrors the `vis_models` saliency seam: **protocol + real backend + offline mock + factory**.\r\n\r\n| Piece | Role |\r\n|---|---|\r\n| `derive_cta_region(html)` | deterministic CTA box from the wireframe markup — **no browser** |\r\n| `MockRenderer` | offline: writes an SVG sketch + the derived region (CI path, dependency-free) |\r\n| `PlaywrightRenderer` | real: headless Chromium → PNG + the actual button bounding box (dev/farm) |\r\n| `build_renderer(backend, offline=…)` | Playwright when available, else the mock |\r\n\r\n```python\r\nfrom rendering import build_renderer\r\nr = build_renderer(\"playwright\")                 # mock fallback if Playwright is absent\r\nres = r.render(wireframe_html, \"out/screen.png\")\r\nres.image_path, res.cta_region                   # feed straight into ux-audit's saliency check\r\n```\r\n\r\nWireframes from the wireframing agent use a known structure (`button.action` / `a.nav`), so the\r\nCTA box is reliable offline. **Real PNGs** need `pip install \"rendering[browser]\"` +\r\n`playwright install chromium` (dev/farm); CI stays on the dependency-free mock.\r\n",
    "assets": [],
    "examples": [],
    "dependencies": [],
    "related": [],
    "contentPath": "",
    "coverPath": "assets/library-card-cover.png",
    "coverPreviewPath": ""
  },
  {
    "slug": "security-review",
    "id": "security-review",
    "name": "security-review",
    "type": "agent",
    "category": "agents",
    "description": "See README.md for details.",
    "shortDescription": "Block for security-review",
    "keywords": [
      "security review"
    ],
    "version": "0.1.0",
    "githubPath": "libraries/agents/blocks/security-review",
    "readme": "# Security Review Agent\r\n\r\n**Agent Core:** `security-review`\r\n\r\n## Overview\r\nThe Security Review Agent serves as the automated security gate in the Sprint pipeline. It performs both static codebase scanning and LLM-driven logical reviews to ensure applications meet minimum security requirements before release.\r\n\r\n## Architecture\r\n- Subclasses the core `Agent` framework from `libraries/agent-core`.\r\n- Plugs into the `sprint` orchestrator pipeline.\r\n- Operates offline and keylessly during local CI testing.\r\n\r\n## Capabilities\r\n- **Static Scanning:** Automatically scans code for common vulnerabilities (e.g., hardcoded credentials, insecure defaults).\r\n- **LLM Security Gate:** Leverages the LLM gateway (`ml-core`) for intelligent analysis of logic flows that standard static analyzers might miss.\r\n",
    "assets": [],
    "examples": [],
    "dependencies": [
      "pydantic>=2.6"
    ],
    "related": [],
    "contentPath": "",
    "coverPath": "assets/library-card-cover.png",
    "coverPreviewPath": ""
  },
  {
    "slug": "sfsymbols",
    "id": "sfsymbols",
    "name": "SF Symbols",
    "type": "library",
    "category": "code-library",
    "description": "Catalog + usage rules for Apple **SF Symbols** (the native icon set). Documents which symbol maps to which action, weight/scale rules, and rendering mode (monochrome / hierarchical / palette) per token. Prefer SF Symbols over custom icons for Apple-native feel and built-in accessibility. No assets to vendor — the symbols ship with the OS; this folder is the **selection + usage policy**, not image files.",
    "shortDescription": "SF Symbols",
    "keywords": [],
    "githubPath": "design-system/sfsymbols",
    "readme": "# SF Symbols\r\n\r\nCatalog + usage rules for Apple **SF Symbols** (the native icon set).\r\n\r\nDocuments which symbol maps to which action, weight/scale rules, and rendering mode\r\n(monochrome / hierarchical / palette) per token. Prefer SF Symbols over custom icons for\r\nApple-native feel and built-in accessibility.\r\n\r\nNo assets to vendor — the symbols ship with the OS; this folder is the **selection +\r\nusage policy**, not image files.\r\n",
    "assets": [],
    "examples": [],
    "dependencies": [],
    "related": [],
    "contentPath": "",
    "coverPath": "assets/library-card-cover.png",
    "coverPreviewPath": ""
  },
  {
    "slug": "skill",
    "id": "skill",
    "name": "skill-name-here",
    "type": "library",
    "category": "code-library",
    "description": "One line — what it does AND when to trigger it. Used for discovery.",
    "shortDescription": "skill-name-here",
    "keywords": [],
    "githubPath": "_templates/skill",
    "skill": "# <Skill Name>\r\n\r\nInstructions the model follows when this skill is active. Be concrete and\r\nstep-by-step. Keep this body short; push detail into `references/`.\r\n\r\n## Steps\r\n1. ...\r\n2. ...\r\n\r\n## Notes\r\n- Deterministic helpers live in `scripts/`.\r\n- Long / optional docs live in `references/` (read on demand).\r\n- Templates, images, fonts live in `assets/`.\r\n",
    "assets": [],
    "examples": [],
    "dependencies": [],
    "related": [],
    "contentPath": "",
    "coverPath": "assets/library-card-cover.png",
    "coverPreviewPath": ""
  },
  {
    "slug": "skill-1",
    "type": "md",
    "category": "skills-library",
    "name": "Make it scroll",
    "shortDescription": "Scroll driven website",
    "description": "Cognitive Shift transforms cutting-edge AI research from MIT and the best practices developed by leading Silicon Valley companies into directly applicable frameworks.",
    "keywords": [
      "skill",
      "website"
    ],
    "contentPath": "content/articles/skill-1/content.md",
    "coverPath": "content/articles/skill-1/cover.png",
    "coverPreviewPath": ""
  },
  {
    "slug": "skill-optimizer",
    "id": "skill-optimizer",
    "name": "skill-optimizer",
    "type": "agent",
    "category": "agents",
    "description": "See README.md for details.",
    "shortDescription": "Block for skill-optimizer",
    "keywords": [
      "skill optimizer"
    ],
    "version": "0.1.0",
    "githubPath": "libraries/agents/blocks/skill-optimizer",
    "readme": "# Skill Optimizer\r\n\r\nBuild-time MLOps platform that optimizes `SKILL.md` files as ML artifacts. Optuna proposes a\r\nSkill configuration → an Agent LLM proposes a variation → a Generator LLM writes the candidate\r\nSkill → the Skill is run to produce an output → a Judge panel scores it → MLflow logs\r\neverything → repeat while `score < goal` → validate on held-out then frozen datasets.\r\n\r\n## Quickstart (offline, no API keys)\r\n\r\n```bash\r\npip install -e \".[dev]\"\r\n\r\n# All commands run with the mock provider — no keys needed.\r\nskill-opt validate-config                       # load + check the 5 YAMLs\r\nskill-opt calibrate-judge --offline             # judge ranks good > bad\r\nskill-opt run --offline --no-track              # one candidate end-to-end\r\nskill-opt optimize-skill --offline --trials 10  # search -> artifacts/best_SKILL.md\r\nskill-opt validate --offline                    # generalization on D_test\r\nskill-opt validate --offline --final            # frozen D_guard final validation\r\nskill-opt portability --offline                 # score preservation across models\r\nskill-opt compress --offline                    # shrink tokens, keep the score\r\n```\r\n\r\nTo go live: put API keys in `.env` (see `.env.example`) and drop `--offline`. The configs\r\nship with real model IDs already.\r\n\r\n**Render-based scoring** (default) judges the *rendered* page, not raw HTML. It needs the\r\nbrowser engine — install once:\r\n\r\n```bash\r\npip install -e \".[render]\"\r\npython -m playwright install chromium\r\n```\r\n\r\nWithout it the runner falls back to judging HTML text (set `execution.mode: static_only` in\r\n`configs/output-metrics.yaml` to force that).\r\n\r\n**Watch a run live:** start the dashboard, then run optimize *with* tracking (no `--no-track`):\r\n\r\n```bash\r\nmlflow ui --backend-store-uri sqlite:///mlflow.db   # open http://127.0.0.1:5000\r\nskill-opt optimize-skill --trials 30                 # watch trials appear\r\n```\r\n\r\n## Layout\r\n\r\n```\r\nconfigs/      the 4 core YAMLs (+ providers.yaml) — the control surface\r\nsrc/skill_optimizer/\r\n  config/     pydantic models that load + validate the YAMLs\r\n  providers/  LiteLLM gateway, generator/judge pools, cost meter, mock provider\r\n  datasets/   D_wm / D_test / D_guard loaders\r\n  genome/     genome <-> SKILL.md assembly\r\n  generator/  Generator LLM -> candidate SKILL.md\r\n  runner/     execute a Skill -> output\r\n  evaluation/ metrics, judge panel, Skill Score\r\n  tracking/   MLflow wrapper + lineage\r\n  cli.py\r\ntests/        unit + integration (mock providers; no real spend in CI)\r\n```\r\n\r\n## Design principles\r\n\r\n- **Evaluation-first.** The judge is the only real risk — build and *calibrate* it before the optimizer exists.\r\n- **Vertical slices, not horizontal layers.** Every slice runs end-to-end.\r\n- **Config is the control surface.** Behaviour changes live in the YAMLs, not in code.\r\n- **Reproducibility by construction.** Every candidate links genome + Optuna trial ID + MLflow run ID + outputs + scores.\r\n- **Cost visible from line one.** Tokens/$ are metered per call and aggregated per trial, under a hard budget cap.\r\n\r\n## Key seams\r\n\r\nEverything depends on three interfaces, so providers, judges, and runners are swappable\r\nand mockable in tests without touching the optimization loop:\r\n\r\n- `providers.LLMGateway.complete(pool, messages) -> Response{text, tokens, cost}`\r\n- `runner.Runner.run(skill) -> Output{artifacts, render?}`\r\n- `evaluation.Judge.score(skill, output) -> Scores`\r\n",
    "skill": "# Skill Optimizer\r\n\r\nA full build-time MLOps platform (not a thin script) that optimizes the factory's own\r\nskills. See **`README.md`** for the architecture, design principles, and key seams.\r\n\r\n## Run it (offline, no keys)\r\n```bash\r\npip install -e \".[dev]\"\r\nskill-opt validate-config\r\nskill-opt calibrate-judge --offline          # judge must rank good > bad, reproducibly\r\nskill-opt optimize-skill --offline --trials 10\r\nskill-opt validate --offline                 # generalization on the held-out set\r\n```\r\nDrop `--offline` and set keys in `.env` (see `.env.example`) to run live.\r\nAn eval dataset root must be supplied — the standalone pilot data is not bundled.\r\n\r\n## Where it fits\r\n- **Meta layer / AI Engineers own it** — it improves skills the Sprint Engineers consume.\r\n- The control surface is the YAMLs in `configs/`; behavior changes live there, not in code.\r\n- Generated output (`mlruns/`, `artifacts/`, `mlflow.db`) is gitignored; only\r\n  source / config / docs / tests are committed.\r\n\r\n## To make it useful for THIS factory\r\nRe-point its eval datasets + objective from the generic pilot task to the factory's real\r\nskills (wireframing prompts, the ux-audit rubric, app-generation skills) on larger, real briefs.\r\n",
    "assets": [],
    "examples": [],
    "dependencies": [
      "pydantic>=2.6",
      "pyyaml>=6.0",
      "litellm>=1.40",
      "optuna>=3.6",
      "mlflow>=2.12",
      "tiktoken>=0.7",
      "typer>=0.12",
      "rich>=13.7",
      "python-dotenv>=1.0"
    ],
    "related": [],
    "contentPath": "",
    "coverPath": "assets/library-card-cover.png",
    "coverPreviewPath": ""
  },
  {
    "slug": "skills",
    "id": "skills",
    "name": "Skill library",
    "type": "library",
    "category": "code-library",
    "description": "Reusable skills built from `_templates/skill` per the `CHARTER.md`. One folder per skill. - Cross-app skills live here. - Project-specific skills may live with their app instead. The four running tools (`wireframing`, `skill-optimizer`, `ux-audit`, `tester`) live under `/agents` because they are agents; pure skills land here.",
    "shortDescription": "Skill library",
    "keywords": [],
    "githubPath": "libraries/skills",
    "readme": "# Skill library\r\n\r\nReusable skills built from `_templates/skill` per the `CHARTER.md`. One folder per skill.\r\n\r\n- Cross-app skills live here.\r\n- Project-specific skills may live with their app instead.\r\n\r\nThe four running tools (`wireframing`, `skill-optimizer`, `ux-audit`, `tester`) live\r\nunder `/agents` because they are agents; pure skills land here.\r\n",
    "assets": [],
    "examples": [],
    "dependencies": [],
    "related": [],
    "contentPath": "",
    "coverPath": "assets/library-card-cover.png",
    "coverPreviewPath": ""
  },
  {
    "slug": "skin-tone-cv",
    "id": "skin-tone-cv",
    "name": "skin-tone-cv (model card)",
    "type": "application",
    "category": "ai-farm",
    "description": "- **Task:** extract skin / hair / eye tones from a selfie → features for seasonal palette. - **Status:** raw → ready (Phase-1 reference model that proves the pipeline). - **Inputs:** image. **Outputs:** tone features / palette class. - **Source / license:** <fill>. - **Ready build:** `../../ready/skin-tone-cv.mlpackage` (CoreML, on-device).",
    "shortDescription": "skin-tone-cv (model card)",
    "keywords": [],
    "githubPath": "libraries/ai-farm/raw/skin-tone-cv",
    "readme": "# skin-tone-cv (model card)\r\n\r\n- **Task:** extract skin / hair / eye tones from a selfie → features for seasonal palette.\r\n- **Status:** raw → ready (Phase-1 reference model that proves the pipeline).\r\n- **Inputs:** image. **Outputs:** tone features / palette class.\r\n- **Source / license:** <fill>.\r\n- **Ready build:** `../../ready/skin-tone-cv.mlpackage` (CoreML, on-device).\r\n",
    "assets": [],
    "examples": [],
    "dependencies": [],
    "related": [],
    "contentPath": "",
    "coverPath": "assets/library-card-cover.png",
    "coverPreviewPath": ""
  },
  {
    "slug": "sprint",
    "id": "sprint",
    "name": "Sprint orchestrator",
    "type": "library",
    "category": "code-library",
    "description": "Chains the factory's agents into **one app sprint** with a single go/no-go verdict. This is the keystone that turns standalone agents into a production line.",
    "shortDescription": "Sprint orchestrator",
    "keywords": [],
    "version": "0.1.0",
    "githubPath": "pipelines/sprint",
    "readme": "# Sprint orchestrator\r\n\r\nChains the factory's agents into **one app sprint** with a single go/no-go verdict. This is the\r\nkeystone that turns standalone agents into a production line.\r\n\r\n```\r\ncharter ─▶ wireframe ─▶ UX audit (+ SUM saliency) ─▶ Apple compliance ─▶ security ─▶ report\r\n           advisory      gate: grade + attention      hard gate: 4.3       hard gate\r\n```\r\n\r\n```python\r\nfrom sprint_orchestrator import SprintOrchestrator\r\nfrom vis_models.saliency import build_saliency\r\n\r\norch = SprintOrchestrator(ctx, out_dir=\"out\", saliency=build_saliency(\"sum\"), min_ux_grade=\"C\")\r\nreport = orch.run({\r\n    \"app\": \"color-matching\",\r\n    \"charter\": \"A weekly color-matching app.\",\r\n    \"descriptor\": {...},                 # for the Apple 4.3 compliance check\r\n    \"code\": {\"ContentView.swift\": \"...\"},# for the security scan\r\n    \"screenshot\": \"screen.png\",          # optional → enables the saliency check\r\n    \"cta_region\": [0.25, 0.80, 0.50, 0.12],\r\n})\r\nreport.passed     # overall go/no-go\r\nreport.summary    # \"color-matching: GO — 3/3 gates passed.\"\r\n```\r\n\r\n## Stages & gates\r\n| Stage | Agent | Blocking? | Passes when |\r\n|---|---|---|---|\r\n| wireframe | `wireframing` | no (advisory) | records the flow grade; agent loops toward A internally |\r\n| ux-audit | `ux-audit` | **yes** | grade ≥ `min_ux_grade` **and** (if a screenshot + saliency model are given) predicted attention lands on the CTA |\r\n| apple-compliance | `apple-compliance` | **yes** | not a Guideline 4.3 spam/duplicate; `passes()` |\r\n| security-review | `security-review` | **yes** | no high-severity finding (incl. iOS: secrets, ATS, Keychain) |\r\n\r\n**Saliency is wired into the UX gate.** Supply `screenshot` + `cta_region` and a `vis_models`\r\nsaliency model and the sprint fails if attention misses the primary action — even when the UX\r\ngrade, compliance, and security all pass. Omit them and the saliency check is skipped (the UX gate\r\nfalls back to grade only).\r\n\r\n## Properties\r\n- All agents share one `AgentContext`, so **spend accrues on one meter** and `report.cost_usd` is\r\n  the whole-sprint cost.\r\n- Every stage runs under the agent-core contract: a failing agent becomes a failed stage, never a\r\n  crash.\r\n- Fully **offline-testable** (mock gateway + mock saliency); writes `out/sprint-report.json`.\r\n",
    "assets": [],
    "examples": [],
    "dependencies": [
      "pydantic>=2.6"
    ],
    "related": [],
    "contentPath": "",
    "coverPath": "assets/library-card-cover.png",
    "coverPreviewPath": ""
  },
  {
    "slug": "sum",
    "id": "sum",
    "name": "sum",
    "type": "model",
    "category": "models",
    "description": "SUM (Saliency Unification through Mamba) predicts a human-attention heatmap for a still image, with a UI/web-page condition tuned for interface screens. Used by ux-audit to check the primary action is the visual hot-spot. Real weights run in the AI-farm; offline, a real pixel-based classical model or a deterministic mock stands in. The adapter is the shared vis_models.saliency core, re-exported here.",
    "shortDescription": "Predicts where viewers look on a still screen or image.",
    "keywords": [
      "image saliency (still-screen attention)"
    ],
    "version": "0.1.0",
    "githubPath": "libraries/models/blocks/sum",
    "readme": "# sum (model card)\r\n\r\nStill-image saliency — where viewers look on a screen\r\n([Arhosseini77/SUM](https://github.com/Arhosseini77/SUM), *Saliency Unification through Mamba*).\r\n\r\n- **Task:** predict a human-attention heatmap for a still screen/image (UI/web-page condition).\r\n- **Inputs:** an image (screen or screenshot). **Outputs:** a `vis_models` `SaliencyMap`.\r\n- **Status:** `raw` — the adapter is the shared `vis_models.saliency` core (used by ux-audit). Real\r\n  `.pth` weights run in the AI-farm (`ai-farm/raw/SUM`); offline it falls back to a real\r\n  pixel-based classical model, or a deterministic mock when forced.\r\n- **Apple-native:** *partial* — Vision's `VNGenerateAttentionBasedSaliencyImageRequest` gives\r\n  general on-device saliency, but not a UI-tuned model; SUM's interface condition is why it's used.\r\n\r\n## Use\r\n```python\r\nfrom blocks.sum.src import build\r\nfrom blocks.sum.src.metrics import analyze\r\nfrom vis_models.saliency import Region\r\n\r\nmodel = build()                              # real SUM on the farm; classical pixel model elsewhere\r\nsmap  = model.predict(\"screen.png\")\r\nanalyze(smap, Region(0.25, 0.80, 0.50, 0.12))   # does attention peak on the primary action?\r\n```\r\n\r\nThe SUM vs ViNet split: **SUM = still screens, ViNet = video/onboarding clips.**\r\n\r\n## Running the real model (AI-farm)\r\nFetch the SUM repo + checkpoint into `ai-farm/raw/SUM/`, install `torch`, then `build()` returns\r\nthe real `SUMSaliencyModel` (`is_available()` gates the fallback).\r\n",
    "assets": [
      {
        "src": "cover.svg",
        "alt": "cover"
      }
    ],
    "examples": [],
    "dependencies": [],
    "related": [],
    "contentPath": "",
    "coverPath": "assets/library-card-cover.png",
    "coverPreviewPath": ""
  },
  {
    "slug": "swiftui-gen",
    "id": "swiftui-gen",
    "name": "swiftui-gen",
    "type": "agent",
    "category": "agents",
    "description": "See SKILL.md for details.",
    "shortDescription": "Block for swiftui-gen",
    "keywords": [
      "swiftui gen"
    ],
    "version": "0.1.0",
    "githubPath": "libraries/agents/blocks/swiftui-gen",
    "skill": "# SwiftUI generation agent\r\n\r\nThe **prototype** step of the design-automation pipeline (deck p.60 step 2): apply the design\r\nsystem onto the wireframe. LLM writes the view; a deterministic checker gates it and, on failure,\r\nre-asks once with the issues.\r\n\r\n```python\r\nfrom swiftui_gen import SwiftUIGenAgent\r\nres = SwiftUIGenAgent(ctx, out_dir=\"apps/color-matching/Sources\").run({\r\n    \"spec\": wireframe_result[\"spec\"],      # {screens: [...]} from the wireframing agent\r\n    \"tokens\": token_names,                  # from design-system/tokens/build/Tokens.swift\r\n    \"app\": \"ColorMatching\",\r\n    \"copy\": {\"onboarding\": {...}},          # optional micro-copy per screen (from microcopy agent)\r\n})\r\nres.output[\"files\"]        # [{screen_id, view, filename, swift, passed, issues}]\r\nres.output[\"all_passed\"]   # gate\r\n```\r\n\r\n**Hard rule:** screens must style **only** with `Tokens.*` — hardcoded colors/fonts/sizes fail the\r\ncheck (they'd break the per-app universe mechanism). Generation is **per screen** so each can be\r\naudited and adjusted independently. See `references/component-map.md` for the element→SwiftUI map.\r\n",
    "assets": [],
    "examples": [],
    "dependencies": [
      "pydantic>=2.6"
    ],
    "related": [],
    "contentPath": "",
    "coverPath": "assets/library-card-cover.png",
    "coverPreviewPath": ""
  },
  {
    "slug": "template",
    "id": "_template",
    "name": "_template",
    "type": "model",
    "category": "models",
    "description": "Longer description: what it does, how, and any caveats.",
    "shortDescription": "one-line description shown in the gallery",
    "keywords": [
      "one line: what this model predicts / produces"
    ],
    "version": "0.0.0",
    "githubPath": "libraries/models/blocks/_template",
    "readme": "# _template (model card)\r\n\r\nReference block — copy this folder to add a model, then fill in `block.json` and `src/`.\r\n\r\n- **Task:** <what it predicts / produces>\r\n- **Inputs / outputs:** <image → mask / features / …>\r\n- **Status:** `raw` (Python adapter + offline mock). Promote to `ready` (CoreML) only when an app\r\n  needs it on device.\r\n- **Source / license:** <repo url> / <license>\r\n- **Apple-native:** <does Vision/CoreML/ARKit already do this? verdict + why the model is still needed>\r\n\r\n## Use\r\n```python\r\nfrom blocks._template.src import build\r\nmodel = build(offline=True)     # deterministic mock (real backend when weights are on the box)\r\nmodel.predict(\"input\")\r\n```\r\n\r\n## Wiring the real backend\r\nImplement `TemplateModel.predict` in `src/model.py` (lazy-import torch/coreml), and make\r\n`is_available()` return True only when deps + weights are present. Until then, `build()` returns\r\nthe mock so the block stays green in CI.\r\n",
    "assets": [],
    "examples": [],
    "dependencies": [],
    "related": [],
    "contentPath": "",
    "coverPath": "assets/library-card-cover.png",
    "coverPreviewPath": ""
  },
  {
    "slug": "tester",
    "id": "tester",
    "name": "tester",
    "type": "agent",
    "category": "agents",
    "description": "See SKILL.md for details.",
    "shortDescription": "Block for tester",
    "keywords": [
      "tester"
    ],
    "version": "0.1.0",
    "githubPath": "libraries/agents/blocks/tester",
    "skill": "# Tester\r\n\r\nCompiles an iOS app scheme, launches it in the simulator, and asserts it stays up (core screen\r\nrenders, no crash on launch). Scope = **compile + launch + one liveness assertion**, not full UI\r\nautomation. The gate is a clean `pass`; a build/launch failure is reported with the log tail, the\r\ncrash-log path, and an LLM-written diagnosis (likely cause + fix).\r\n\r\n## Layers\r\n- `src/tester/runner.py` — deterministic `smoke_test(app_dir, scheme, bundle_id)`. Orchestrates the\r\n  two shell scripts, parses the result, and returns a structured `SmokeReport`. No LLM. The command\r\n  runner is injectable, so the logic is unit-tested offline (no Mac) in CI.\r\n- `src/tester/agent.py` — `TesterAgent` on the agent-core contract (budget, cost, tracking). Runs\r\n  the smoke test; on a **build-failed / launch-failed** result it asks the model for a short\r\n  diagnosis. A passing run spends no tokens.\r\n- `scripts/build.sh` / `scripts/smoke.sh` — the real macOS `xcodebuild` + `simctl` capability.\r\n\r\n## Run it\r\n```\r\npython -m tester [APP_DIR] [SCHEME] [BUNDLE_ID]     # deterministic core, prints status\r\npython -m tester --json                              # full SmokeReport as JSON\r\n```\r\nExit code: `0` pass · `1` build/launch failure · `2` unavailable (not macOS / no Xcode) or error.\r\n\r\n`status` separates a broken app (`build-failed` / `launch-failed`) from \"can't run here\"\r\n(`unavailable`), so off-macOS runs are inconclusive rather than false failures.\r\n",
    "assets": [],
    "examples": [],
    "dependencies": [
      "pydantic>=2.6"
    ],
    "related": [],
    "contentPath": "",
    "coverPath": "assets/library-card-cover.png",
    "coverPreviewPath": ""
  },
  {
    "slug": "ux_attention_pattern",
    "id": "ux_attention_pattern",
    "name": "ux-attention-pattern",
    "type": "library",
    "category": "code",
    "description": "Tuned for iOS app screens (narrow, vertical, component-driven), grounded in NN/g eye-tracking research + Apple HIG. Uses the attention modules (vis_models.saliency — real SUM when weights are on the box, else the classical pixel model, else the deterministic mock) to predict a screen's attention, then classifies the reading flow from the grid's row/column profiles: vertical (layer-cake column of sections — good), gutenberg (top anchor + bottom terminal CTA in the thumb zone — good), focused (single hero/action — good), f-skim (top-left text-wall skim — warn), scattered (no hierarchy — bad). If a cta point is given, a gutenberg/vertical screen is only fully good when attention reaches the CTA. The rule core (classify_pattern) is pure stdlib; the image layer depends on vis_models.saliency.",
    "shortDescription": "Run a screen through the attention modules and decide which iOS reading flow it follows, and whether that's healthy.",
    "keywords": [
      "classify an iOS screen's reading pattern (vertical/gutenberg/f-skim/focused/scattered) and judge it"
    ],
    "version": "0.1.0",
    "githubPath": "libraries/code/blocks/ux_attention_pattern",
    "readme": "# ux-attention-pattern (code block)\r\n\r\nDecides **which reading pattern an iOS screen follows — and whether it's healthy.** The attention\r\ncompanion to `ux-position-mask` (geometry): this one uses the *attention* signal. Tuned for iOS\r\n(narrow, vertical, component-driven), grounded in NN/g eye-tracking research + Apple HIG.\r\n\r\n- **`audit_attention(path, backend=\"sum\")`** — runs the screen through the attention modules\r\n  (`vis_models.saliency`: real **SUM** on the farm, classical/mock offline) → attention map → classify.\r\n- **`classify_pattern(values, width, height, cta=None)`** — the pure, stdlib rule core (the\r\n  `entry`): reads the grid's row/column profiles and classifies the flow. If a `cta` point is given,\r\n  a gutenberg/vertical screen is only fully *good* when attention reaches the CTA in the thumb zone.\r\n\r\n## The iOS pattern set\r\n| Pattern | Screen | Verdict |\r\n|---|---|---|\r\n| **vertical** | centered column of sections (layer-cake: nav title → cards → tab bar) | ✅ good |\r\n| **gutenberg** | top anchor + bottom terminal CTA (onboarding / paywall / hero) | ✅ good |\r\n| **focused** | one dominant hero/action | ✅ good |\r\n| **f-skim** | top-left concentration on an unstructured text wall | ⚠️ warn — add sections/bold |\r\n| **scattered** | no column, order, or terminal action | ❌ bad |\r\n\r\n*Web F/Z collapse into vertical motion on phones: F → an f-skim, Z → a gutenberg top-to-bottom flow.*\r\n\r\n**Output:** `{ pattern, level (good|ok|warn|bad), good, confidence, focal, dispersion, features, reasons }`.\r\n\r\n**Depends on** `vis_models.saliency` (the SUM/ViNet attention modules). The rule core has no deps and\r\nis always exercised in CI; the image layer runs offline via the mock. On real screens, the classical\r\nfallback is diffuse — accurate attention needs the SUM weights on the AI-farm.\r\n",
    "assets": [
      {
        "src": "cover.svg",
        "alt": "cover"
      }
    ],
    "examples": [],
    "dependencies": [],
    "related": [],
    "contentPath": "",
    "coverPath": "assets/library-card-cover.png",
    "coverPreviewPath": ""
  },
  {
    "slug": "ux_position_mask",
    "id": "ux_position_mask",
    "name": "ux-position-mask",
    "type": "library",
    "category": "code",
    "description": "Audits which elements of a screen are in good vs dangerous positions. Core rules (`audit`, pure stdlib): given elements with a role (primary/secondary/content) and a normalized box, return a per-element verdict (good/ok/dangerous) with reasons plus a score/grade — thumb-reach ergonomics (primary actions belong lower-center), rule-of-thirds active zones (key content where the eye lands first), and safe areas (status bar/notch, home indicator, edges). For an arbitrary screen *design*, `audit_image(path)` first runs a classical, offline detector (background subtraction + connected components + role heuristics; Pillow/numpy/scipy) to find the elements, then scores them. Migration path: when auditing *generated* screens, replace detection by reading element boxes from the known layout — the rules stay the same.",
    "shortDescription": "Audit a screen design: detect its elements and flag which sit in good/dangerous positions (thumb reach, active zones, safe areas).",
    "keywords": [
      "audit which screen elements sit in good vs dangerous positions"
    ],
    "version": "0.1.0",
    "githubPath": "libraries/code/blocks/ux_position_mask",
    "readme": "# ux-position-mask (code block)\r\n\r\nAudits a screen design: **which elements sit in good vs dangerous positions.** Two layers:\r\n\r\n- **`audit(elements)`** — the pure, stdlib rules engine. Maps each element onto a UX zone mask\r\n  (thumb reach, rule-of-thirds active zones, safe areas) → per-element verdict (good / ok /\r\n  dangerous) + reasons + score/grade. Inputs: `[{name, role, box:[x,y,w,h]}]`, normalized `[0,1]`,\r\n  origin top-left. Roles: `primary` (main CTA), `secondary` (supporting), `content` (hero/text/media).\r\n- **`audit_image(path)`** — for an arbitrary screen **design/screenshot**: a classical, offline\r\n  detector (background subtraction → connected components → role heuristics; Pillow/numpy/scipy)\r\n  finds the elements, then `audit` scores them. Best-effort — correct it in the tester UI.\r\n\r\n**Outputs:** `{ elements:[{verdict, reach, attention, reasons}], flags, score, grade, detected }`.\r\n\r\n**First needed by:** UX auditing (code.md Task 4). Complements the `ux-audit` agent's saliency check.\r\n\r\n**Migration path:** when auditing *generated* screens, drop `audit_image`'s detection and feed the\r\nelement boxes from the known layout into `audit` — the rules don't change.\r\n\r\n## The rules\r\nGrounded in the **Design Composition** knowledge corpus (`../../../knowledge/composition` — active\r\nvs passive zones, rule of thirds: *\"place header text, hero images, or CTA buttons in the active\r\nzones\"*) and the standard mobile thumb-zone reachability map.\r\n\r\n- **Thumb reach** (radial, one-handed — the dominant ~49% grip): a circular comfort zone centered\r\n  in the **lower-middle** of the screen (where the thumb rests and sweeps). The green **EASY**\r\n  circle sits lower-center and reaches up to about the middle; the yellow **OK** ring surrounds it;\r\n  the **top — especially the top corners — is red HARD** (out of reach one-handed). Primary actions\r\n  belong in the easy circle.\r\n- **Active zones** (rule of thirds / F-pattern top-weighting): the upper region and thirds lines get\r\n  the most attention → key content belongs there; passive zones get less.\r\n- **Safe areas**: elements crossing the status bar/notch, the home indicator, or the screen edges\r\n  are flagged dangerous (may be clipped or hard to tap).\r\n\r\n## Use\r\n```python\r\nfrom blocks.ux_position_mask.src.ux_position_mask import audit\r\naudit([{\"name\": \"Buy\", \"role\": \"primary\", \"box\": [0.2, 0.82, 0.6, 0.08]}])\r\n# -> primary action in the natural thumb zone -> \"good\"\r\n```\r\n\r\nGrounded in `../../../knowledge/composition` (active/passive zones, rule of thirds). Deterministic\r\nand offline — no model, no LLM.\r\n",
    "assets": [
      {
        "src": "cover.svg",
        "alt": "cover"
      }
    ],
    "examples": [],
    "dependencies": [],
    "related": [],
    "contentPath": "",
    "coverPath": "assets/library-card-cover.png",
    "coverPreviewPath": ""
  },
  {
    "slug": "ux-audit",
    "id": "ux-audit",
    "name": "ux-audit",
    "type": "agent",
    "category": "agents",
    "description": "See README.md for details.",
    "shortDescription": "Block for ux-audit",
    "keywords": [
      "ux audit"
    ],
    "version": "0.1.0",
    "githubPath": "libraries/agents/blocks/ux-audit",
    "readme": "# UX Audit Agent\r\n\r\n**Agent Core:** `ux-audit`\r\n\r\n## Overview\r\nThe UX Audit Agent acts as an automated design critic in the Sprint pipeline. It reviews wireframes and user flows against an established UX checklist to ensure that every application ships with an intuitive and Apple-native user experience.\r\n\r\n## Architecture\r\n- Subclasses the core `Agent` framework from `libraries/agent-core`.\r\n- Plugs into the `sprint` orchestrator pipeline.\r\n- Uses `libraries/models/blocks/sum` (Saliency) as an external tool, allowing saliency-deferred auditing (computing visual focus offline before making UX judgments).\r\n\r\n## Capabilities\r\n- **UX Checklist Review:** Evaluates designs against Lean AI Factory standards.\r\n- **Visual Saliency Integration:** Identifies UI blind spots by analyzing focal points via the SUM (Saliency in User interfaces) model.\r\n",
    "skill": "# UX audit\r\n\r\nA real agent (`src/ux_audit/`, on `agent-core`): scores a screen 0-5 on the ten HIG-anchored\r\nchecklist criteria (`references/ux-checklist.md`, incl. `hig` conformance) and grades A-F.\r\n\r\n```python\r\nfrom ux_audit import UXAuditAgent\r\nres = UXAuditAgent(ctx).run(screen_html_or_description)   # res.output[\"grade\"], [\"scores\"], [\"average\"]\r\n```\r\n\r\n**Saliency attention check — live, from real pixels.** Add a rendered screenshot and the audit\r\npredicts an attention heatmap, checks it peaks on the primary action, and embeds a colour-mapped\r\nheatmap image in the report:\r\n\r\n```python\r\nres = UXAuditAgent(ctx).run({\"screen\": html, \"screenshot\": \"screen.png\",\r\n                             \"cta_region\": [0.25, 0.80, 0.50, 0.12]})\r\nres.output[\"saliency\"]   # {status, passes, primary_attention, focal_point, heatmap: \"data:image/png;base64,...\"}\r\n```\r\n\r\nBackends (`vis_models.saliency.build_saliency`), best → simplest:\r\n- **SUM** — deep model for stills; used when its weights are in `libraries/ai-farm/raw/SUM`.\r\n- **classical** *(default)* — a real, pixel-based center-surround + edge saliency, **stdlib-only**,\r\n  so it runs anywhere (no torch/weights). This is what `UXAuditAgent` auto-builds when you pass a\r\n  screenshot but no model.\r\n- **mock** — deterministic, pixel-ignoring; for unit tests/demos (`offline=True`).\r\n\r\nThe report renders the heatmap (cool = ignored, warm = high attention, white box = the CTA) so it\r\n**shows the weak areas**, not just scores them. **ViNet (onboarding video) stays deferred.**\r\n",
    "assets": [],
    "examples": [],
    "dependencies": [
      "pydantic>=2.6"
    ],
    "related": [],
    "contentPath": "",
    "coverPath": "assets/library-card-cover.png",
    "coverPreviewPath": ""
  },
  {
    "slug": "vinet",
    "id": "vinet",
    "name": "vinet",
    "type": "model",
    "category": "models",
    "description": "ViNet-S (ViNet++, U-Net decoder over an S3D backbone) predicts a per-frame human-attention heatmap for a video. Targets the maintained successor because the original ViNet's weights are no longer published; ViNet-S is the same task, ~36MB, and CPU-runnable. Used to check that an onboarding clip leads the eye toward the primary action over time. Real weights run in the AI-farm (or any torch box); a deterministic mock stands in for CI.",
    "shortDescription": "Predicts where viewers look across the frames of a video clip.",
    "keywords": [
      "video saliency (per-frame attention for a clip)"
    ],
    "version": "0.1.0",
    "githubPath": "libraries/models/blocks/vinet",
    "readme": "# vinet (model card)\r\n\r\nVideo saliency — per-frame human-attention heatmaps for a clip. Targets **ViNet-S**\r\n([ViNet++ / vinet_v2](https://github.com/ViNet-Saliency/vinet_v2), U-Net decoder over an S3D\r\nbackbone).\r\n\r\n> **Why ViNet-S, not the original ViNet:** the original ViNet's pretrained weights are no longer\r\n> published (dead link). ViNet-S is its maintained successor — same task, ~36MB, CPU-runnable.\r\n\r\n- **Task:** predict where viewers look across the frames of a video (e.g. an onboarding clip).\r\n- **Inputs:** a clip — a folder of frames, or a list of frame paths. ViNet-S uses a 32-frame\r\n  window per forward; the adapter slides it to yield one map per frame.\r\n- **Outputs:** one `vis_models` `SaliencyMap` per frame; `metrics.analyze_clip` reduces the\r\n  sequence to \"does attention settle on the primary action by the end?\".\r\n- **Status:** `raw` — Python adapter + offline mock. Real `.pt` weights run in the AI-farm\r\n  (`ai-farm/raw/ViNet`); CI uses the deterministic mock.\r\n- **Apple-native:** none — Apple exposes no video attention/saliency-prediction API.\r\n\r\n## Use\r\n```python\r\nfrom blocks.vinet.src import build\r\nfrom blocks.vinet.src.metrics import analyze_clip\r\nfrom vis_models.saliency import Region\r\n\r\nmodel = build()                              # real ViNet-S when torch + weights are on the box\r\nmaps  = model.predict(\"clip/frames\")         # list[SaliencyMap], one per frame\r\nanalyze_clip(maps, Region(0.30, 0.72, 0.40, 0.16))   # -> settles_on_cta, mean/final attention, …\r\n```\r\n\r\n## Running the real model (AI-farm / any torch box)\r\nFetch the `vinet_v2` repo and a ViNet-S checkpoint (`vinet_s_dhf1k.pt`, from the repo's Google\r\nDrive bundle) under `ai-farm/raw/` — the adapter auto-discovers `ViNet_S_model.py` and a\r\n`vinet_s*.pt` weight. Then `build()` returns the real `ViNetModel` (`is_available()` gates the\r\nfallback). Preprocessing (224×384 + ImageNet norm, 32-frame clip) matches the repo; verified\r\nrunning on CPU.\r\n",
    "assets": [
      {
        "src": "cover.svg",
        "alt": "cover"
      }
    ],
    "examples": [],
    "dependencies": [],
    "related": [],
    "contentPath": "",
    "coverPath": "assets/library-card-cover.png",
    "coverPreviewPath": ""
  },
  {
    "slug": "vision_camera",
    "id": "vision_camera",
    "name": "vision-camera",
    "type": "library",
    "category": "code",
    "description": "On-device capture and privacy helpers seeded by the Color-Matching app. ImageCrypto.swift encrypts a captured image at rest (CryptoKit) before any persistence; a SwiftUI capture view (AVCaptureSession) is planned. Every 'scan something' niche (color, skin, plant, food) repeats capture + encryption — written once here, imported by apps. Swift block: reused on-device, not exposed as a Python entry.",
    "shortDescription": "Reusable SwiftUI capture + CryptoKit image encryption for any on-device 'scan something' niche.",
    "keywords": [
      "on-device camera capture + image encryption at rest"
    ],
    "version": "0.1.0",
    "githubPath": "libraries/code/blocks/vision_camera",
    "readme": "# vision-camera (code snippet)\r\n\r\nReusable on-device capture + privacy helpers, seeded by Sprint 1 (Color Matching).\r\nAny app that takes a photo and analyzes it on-device should reuse these.\r\n\r\n## Contents (to add)\r\n- `CameraView.swift` - SwiftUI `UIViewControllerRepresentable` wrapping `AVCaptureSession` for a still capture.\r\n- `ImageCrypto.swift` - encrypt a captured image at rest (CryptoKit) before any persistence.\r\n\r\n## Why it's a library\r\nEvery \"scan something\" niche (color, skin, plant, food) repeats capture + encryption.\r\nWrite once here; apps import it. See `../README.md` for the rule.\r\n",
    "assets": [
      {
        "src": "cover.svg",
        "alt": "cover"
      }
    ],
    "examples": [],
    "dependencies": [],
    "related": [],
    "contentPath": "",
    "coverPath": "assets/library-card-cover.png",
    "coverPreviewPath": ""
  },
  {
    "slug": "wireframe-studio",
    "id": "wireframe-studio",
    "name": "Wireframing Studio",
    "type": "library",
    "category": "code",
    "description": "A production-grade, human-driven wireframing tool for the Lean AI Factory. It's the **editable-flow surface** the `libraries/mcp/wireframing` README promises — a person places elements, links screens, and adjusts the flow, with a **live A–F flow grade** that mirrors the factory's automated audit. Zero dependencies, no build step, no server. It's a single self-contained `index.html`.",
    "shortDescription": "Wireframing Studio",
    "keywords": [],
    "githubPath": "libraries/code/wireframe-studio",
    "readme": "# Wireframing Studio\r\n\r\nA production-grade, human-driven wireframing tool for the Lean AI Factory. It's the\r\n**editable-flow surface** the `libraries/mcp/wireframing` README promises — a person places\r\nelements, links screens, and adjusts the flow, with a **live A–F flow grade** that mirrors the\r\nfactory's automated audit.\r\n\r\nZero dependencies, no build step, no server. It's a single self-contained `index.html`.\r\n\r\n## Run\r\n\r\nJust open it:\r\n\r\n```bash\r\n# Windows\r\nstart index.html\r\n# macOS\r\nopen index.html\r\n# or double-click index.html in a file browser\r\n```\r\n\r\n(Optional) serve it if your browser restricts `file://`:\r\n\r\n```bash\r\npython -m http.server 8000   # then visit http://localhost:8000/libraries/code/wireframe-studio/\r\n```\r\n\r\nEverything autosaves to the browser's `localStorage`. Use **Import / Export** to move a design\r\nbetween machines or into the pipeline.\r\n\r\n### If it ever loads blank\r\n\r\nThe app self-heals: if a saved state fails to render, it automatically discards it and loads a\r\nclean project. If you're on an older cached copy, **hard-refresh** (`Ctrl+Shift+R`) to pick up\r\nthe latest, or click **Reset & reload** on the error banner. **New** (top bar) always gives you a\r\nblank project. Rendering is section-isolated and wrapped in a global error handler, so a single\r\nfailure can never freeze the whole tool.\r\n\r\n## What it does\r\n\r\n- **Canvas of screens** — drag to arrange, pan (drag empty space), zoom (Ctrl/⌘+scroll or the zoom bar), **Fit** to frame all.\r\n- **iOS-aware component palette** — nav bar, tab bar, title, text, button, list link, list, text field, search, toggle, image, card. Click to add to the selected screen.\r\n- **Inspector** — edit labels, placeholders, list rows, back button, tab bars; mark a button's **Action**; set an element's **link → target screen** (or \"Draw link on canvas\" then click the target).\r\n- **Live flow audit** (right panel) — recomputed on every edit, showing the grade, the exact failing issues, and metrics.\r\n- **Undo/redo** (Ctrl/⌘+Z · Ctrl/⌘+Shift+Z), **Delete** to remove the selection, **Esc** to cancel linking.\r\n\r\n## Flow audit — same rules as the factory\r\n\r\nThe live grade uses the **same thresholds** as `agents/wireframing/src/wireframing/audit.py`:\r\n\r\n| Rule | Threshold |\r\n|---|---|\r\n| Clicks to a key action | ≤ 3 |\r\n| Interactive features per screen | ≤ 7 |\r\n| Max navigation depth | ≤ 4 |\r\n| Orphan screens (unreachable) | 0 |\r\n| Dead links (target missing) | 0 |\r\n\r\nGrade `A` = none of the above violated **and** at least one primary Action is defined.\r\nEach additional issue drops the grade `A → B → C → D → F`, matching the Python audit exactly.\r\n\r\n> A link is only a link when you draw it. That's what kills the classic hallucinated-flow\r\n> failure mode (buttons to nowhere, unlinked pages) — you can't create a dead end by accident,\r\n> and the audit flags any you do create.\r\n\r\n## Exports\r\n\r\n| Export | File | Use |\r\n|---|---|---|\r\n| **Studio spec** | `*.studio.json` | Full-fidelity, re-importable (canvas positions + components). |\r\n| **Factory WireframeSpec** | `*.wireframe.json` | Directly consumable by `wireframing/generate.py` + `audit.py`. Shape matches `WireframeSpec` (screens → `id, title, elements[], links[], actions[]`). The start screen is emitted as `index` so the factory auditor's default entry (`index.html`) grades from the correct screen. Verified: export → `generate.py` → `audit.py` returns **grade A**. |\r\n| **Interactive prototype** | `*.prototype.html` | Self-contained clickable phone prototype — share it, click through the real flow. |\r\n| **SwiftUI scaffold** | `*.swift` | Structural handoff for Sprint Engineers: a `NavigationStack` + one `View` per screen, `NavigationLink`s for drawn links. |\r\n| **Wireframe image** | `*.svg` / `*.png` | Flat snapshot of the flow for docs/retros. |\r\n\r\nImport accepts either a **Studio spec** or a plain **factory WireframeSpec** (it rebuilds an\r\neditable model from the latter), so it round-trips with the existing `agents/wireframing`\r\noutput.\r\n\r\n## How it fits the factory\r\n\r\n```\r\ncharter ─▶ WireframingAgent (LLM draft) ─▶ *.wireframe.json ─┐\r\n                                                             ├─▶ Wireframing Studio (human adjusts)\r\nprior work / from scratch ───────────────────────────────────┘        │\r\n                                                                        ├─▶ *.wireframe.json ─▶ generate.py / audit.py\r\n                                                                        ├─▶ *.prototype.html (review the flow)\r\n                                                                        └─▶ *.swift (SwiftUI handoff)\r\n```\r\n\r\nThe agent can draft a flow headlessly; a human opens the Studio to adjust it (add a button →\r\ntarget, rename, add content) exactly as the MCP README describes; the \"A\"-grade result flows\r\nback into the sprint gate chain (ux-audit → compliance → security) unchanged.\r\n\r\n## Data model\r\n\r\nCompatible superset of `WireframeSpec`. A screen carries canvas position + typed components:\r\n\r\n```jsonc\r\n{\r\n  \"meta\": { \"name\": \"My App\", \"entry\": \"home\" },\r\n  \"screens\": [\r\n    {\r\n      \"id\": \"home\", \"title\": \"Home\", \"x\": 60, \"y\": 60,\r\n      \"comps\": [\r\n        { \"id\": \"c1\", \"kind\": \"navbar\", \"label\": \"My App\", \"props\": {} },\r\n        { \"id\": \"c2\", \"kind\": \"button\", \"label\": \"Get started\",\r\n          \"props\": {}, \"action\": \"start\", \"linkTo\": \"onboard\" }\r\n      ]\r\n    }\r\n  ]\r\n}\r\n```\r\n\r\n`toFactorySpec()` projects this down to the exact `{screens:[{id,title,elements,links,actions}]}`\r\nthe Python generator/auditor expect — no logic is duplicated; the JS audit intentionally\r\nmirrors `audit.py`'s thresholds so both tools agree on a grade.\r\n\r\n## Tests\r\n\r\nThe core logic (audit grading, factory projection, exports) is validated headlessly with a\r\nsmall Node + DOM-shim harness — grade-A sample, dead-link/orphan/over-feature detection,\r\nprototype/SwiftUI generation, and a factory export→import→re-audit round trip. See the commit\r\nnotes; the tool itself needs no runtime.\r\n",
    "assets": [],
    "examples": [],
    "dependencies": [],
    "related": [],
    "contentPath": "",
    "coverPath": "assets/library-card-cover.png",
    "coverPreviewPath": ""
  },
  {
    "slug": "wireframing",
    "id": "wireframing",
    "name": "wireframing",
    "type": "library",
    "category": "mcp",
    "description": "See README.md for details.",
    "shortDescription": "Block for wireframing",
    "keywords": [
      "wireframing"
    ],
    "version": "0.1.0",
    "githubPath": "libraries/mcp/blocks/wireframing",
    "readme": "# Wireframing MCP\r\n\r\nThe MCP server behind the wireframing skill: given a project charter + screen spec, it places\r\nelements per page, links screens, and returns the wireframe plus an **editable flow** a human can\r\nadjust (add a button → target, rename, add content). The `agents/wireframing` skill drives this\r\nserver; the `wireframe-studio` editor is the human face of the same shared `WireframeSpec`.\r\n\r\nGrading and HTML generation are **reused** from `agents/wireframing` (`spec` / `generate` / `audit`) —\r\none source of truth, not reimplemented here.\r\n\r\n## Layout\r\n```\r\nlibraries/mcp/wireframing/\r\n├── src/wireframing_mcp/\r\n│   ├── core.py      # pure tool logic (offline): new/add_screen/place_element/add_action/\r\n│   │                #   link_screens/unlink/remove_screen/rename_screen/audit/export_html\r\n│   ├── server.py    # MCP transport — exposes core as MCP tools (FastMCP)\r\n│   └── bridge.py    # HTTP bridge (CORS) so the browser studio shares the same spec\r\n└── tests/test_core.py\r\n```\r\n\r\n## Run\r\n\r\n**Tests** (offline, no keys, no MCP SDK):\r\n```bash\r\ncd libraries/mcp/wireframing && python -m pytest -q\r\n```\r\n\r\n**MCP server** (for agents — Codex / Claude):\r\n```bash\r\npip install \"wireframing-mcp[server]\"     # installs the `mcp` transport\r\npython -m wireframing_mcp.server\r\n```\r\nTools: `new_wireframe`, `add_screen`, `rename_screen`, `place_element`, `add_action`,\r\n`link_screens`, `unlink`, `remove_screen`, `audit`, `export_html`, `get_spec`, `load_spec`.\r\n\r\n**HTTP bridge** (for the human `wireframe-studio` editor — stdlib only, no install):\r\n```bash\r\npython -m wireframing_mcp.bridge          # http://localhost:8765\r\n```\r\n`GET /spec` · `POST /spec` · `GET /audit` · `GET /health`. In the studio, click **⇅ MCP** →\r\n**Push** / **Pull** to share the flow. The shared contract is the factory `WireframeSpec`\r\n(`{screens:[{id,title,elements,links,actions}]}`); the studio projects its rich layout to/from it.\r\n\r\n## How it fits\r\n```\r\ncharter ─▶ wireframing agent ─┐\r\n                              ├─▶ Wireframing MCP (one shared WireframeSpec) ─▶ audit (A–F) ─▶ export HTML\r\nwireframe-studio (human) ─────┘        ▲ reuses agents/wireframing spec/generate/audit\r\n```\r\n`navigation-flow` (sibling MCP) grades a flow; this server's `audit` uses the same canonical logic.\r\n",
    "assets": [],
    "examples": [],
    "dependencies": [
      "pydantic>=2.6"
    ],
    "related": [],
    "contentPath": "",
    "coverPath": "assets/library-card-cover.png",
    "coverPreviewPath": ""
  }
]